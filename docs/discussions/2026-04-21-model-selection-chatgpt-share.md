# Модели для генерации теней

- Source URL: https://chatgpt.com/share/69e75e59-c638-8386-bc0d-36fef1dc7307
- Capture date: 2026-04-21
- Capture format: visible ChatGPT share conversation, turn-by-turn text only.

## Turn 1 - User

Я для своего проекта хочу улучшить модель генерации этих самых теней. И раньше у меня был какой-то, в общем, простой пикс-ту-пик, а хочу сделать диффузионку. И вот хочу не какой-нибудь простой Stable Diffusion с Торатом это обучить, а что вообще есть современного, чтобы при этом обучалось все, в идеале, в 11 гигов оперативки, но можно типа в 4090 влезало, сколько там, 24 гига оперативки. В общем, надо посмотреть, какие есть диффузионки, какие-то, ну, какие-то более интересные, чем простой Stable Diffusion, которые можно дообучить. Вот я хочу, чтобы на вход подавалась картинка там и маска, и эту картинку переносила на белый фон, рисовалась тень такая прям реалистичная, чтобы не только вид сверху был, но еще другие виды. В общем, такое, надо подумать, какую модель для этого использовать. В 2к26

## Turn 2 - Assistant

Для твоей задачи (вход: объект + маска → выход: белый фон + реалистичная тень) в 2026 уже есть намного более интересные архитектуры, чем классический Stable Diffusion + ControlNet. Ниже — концентрированный обзор того, что реально стоит рассматривать под 24GB VRAM и даже ≈11GB с компромиссами.

🧠 Ключевая идея для твоей задачи

Тебе нужен не просто text-to-image diffusion, а:

conditional diffusion

image-to-image

mask-conditioned

желательно multi-condition (image + mask + geometry)

Это сейчас называется task-conditioned / conditional diffusion — когда модель генит изображение строго по входным условиям (маска, глубина, изображение и т.д.).
ResearchGate

Именно такие модели лучше всего подходят для генерации теней.

🥇 Самые подходящие архитектуры (2026)
1. DiT-based модели (Flux / SD3 / новые)

👉 лучший баланс качества + controllability

Что смотреть:

FLUX.1 / FLUX.2

Stable Diffusion 3 / SD3 Medium

HiDream-I1

Qwen-Image

Эти модели сейчас считаются основными open-source конкурентами.
Labellerr

Почему они лучше SDXL:

лучше spatial reasoning

лучше свет/тени

лучше conditioning

transformer backbone (DiT)

💡 Для теней это прям критично.

VRAM:

inference: ~10–14GB (fp16)

fine-tune: 24GB (LoRA) — нормально

full fine-tune: уже тяжело

👉 мой топ-кандидат: FLUX + LoRA + image conditioning

2. Conditional Diffusion (mask-conditioned)

Это уже прямо под твою задачу.

Есть исследования:

Mask Conditional Diffusion Models

explicit mask embedding

multi-condition input

Например, mask-conditioned diffusion позволяет генерировать изображение строго по форме объекта.
arXiv

Это почти 1:1 твой pipeline:

input:
 - object image
 - mask
 - optional: depth
output:
 - shadow
3. ControlNet нового поколения (но не классический)

Не старый ControlNet, а:

T2I-Adapter

ControlNet v2

IP-Adapter

Multi-ControlNet

Эти методы — это conditioning поверх diffusion, а не просто prompt.
Conditional diffusion control сейчас активно используется для точного управления генерацией.
shadecoder.com

💡 Можно сделать:

mask → control

object image → reference

depth → geometry

4. Image editing diffusion (очень подходит)

Современные модели умеют:

inpainting diffusion

compositional diffusion

guided editing

Например, Explicit Conditioning снижает compute и ускоряет inference для image editing diffusion.
arXiv

Это идеально для:

object + mask → edit → add shadow
🥇 Практический рейтинг для твоего проекта
🥇 Лучший вариант (мой выбор)

FLUX + multi-conditioning

image conditioning

mask conditioning

optional depth

LoRA fine-tune

Почему:

хороший geometry reasoning

хорошая работа со светом

controllable

🥈 Второй вариант

SD3 Medium + ControlNet v2
проще обучить
экосистема уже есть

🥉 Самый инженерный вариант

Custom conditional diffusion (UNet small)

без текста

только image + mask

плюсы:

можно обучить на 11GB

быстрее

стабильнее

минусы:

меньше универсальности

🧠 Архитектура, которую я бы сделал
Input:
 - RGB object
 - binary mask
 - optional: depth (MiDaS)

Condition encoder:
 - image encoder (CLIP / DINOv2)
 - mask embedding
 - geometry embedding

Diffusion backbone:
 - DiT (Flux-like)
 - or UNet (lightweight)

Output:
 - shadow layer
 - alpha
🧪 Альтернатива (очень практичная)

Не генерить сразу финальную картинку, а:

генерить:

shadow map
depth-aware shadow

а потом:

composite(object + white background + shadow)

Это:

проще учить

меньше артефактов

меньше VRAM

🧠 Что влезет в VRAM
11GB

lightweight UNet diffusion

SD1.5 fine-tune

custom conditional diffusion

LoRA на маленькой модели

24GB (4090)

FLUX LoRA

SD3 LoRA

multi-condition training

ControlNet training

🧪 Минимальный practical pipeline

Я бы начал с:

SDXL или SD3

image-to-image

mask conditioning

LoRA fine-tune

dataset:

object

mask

shadow GT

🧠 Но есть ещё более правильный вариант (лучший для теней)

👉 diffusion только для shadow

input:
 object
 mask

output:
 shadow-only map

Это сильно:

уменьшает пространство

ускоряет обучение

лучше generalization

Если коротко (что выбрать)

Лучший стек 2026 для твоей задачи:

🥇 FLUX + LoRA + mask conditioning

🥈 SD3 + ControlNet v2

🥉 Custom conditional diffusion (UNet)

Если скажешь:

dataset размер

сколько пар object→shadow

real или synthetic

нужна ли генерация ракурса

Я скажу конкретную архитектуру + training config.

## Turn 3 - User

А какая то модель от энвидии?
Что то там z.

## Turn 4 - Assistant

Сейчас быстро проверю актуальные модели NVIDIA и попробую понять, что именно у них на «z» ты, скорее всего, имеешь в виду. Заодно отфильтрую то, что реально подходит под дообучение, а не просто красиво звучит.

Скорее всего, ты имеешь в виду SANA от NVIDIA. У NVIDIA Research есть проект SANA: Efficient High-Resolution Image Synthesis with Linear Diffusion Transformer. Они позиционируют его как эффективную text-to-image diffusion-модель, способную генерировать изображения до 4096×4096 и при этом быть достаточно лёгкой для запуска даже на laptop GPU. У проекта есть страница NVIDIA Research и открытый код/model card.
NVIDIA
+2
Hugging Face
+2

Но для твоей задачи есть важный нюанс: SANA из коробки — это прежде всего text-to-image, а не специализированная image-conditioned модель под композицию объекта, маски и тени. То есть как базовая архитектура она интересная, особенно из-за своей эффективности, но под твой pipeline её всё равно придётся довольно заметно адаптировать: добавлять conditioning на mask / reference image / possibly depth, либо строить отдельную голову под shadow map.
NVIDIA
+1

Почему SANA вообще может быть интересна тебе:

у неё Linear Diffusion Transformer, сделанный с упором на эффективность;

NVIDIA прямо подчёркивает, что модель рассчитана на более экономную работу по сравнению с обычными тяжёлыми diffusion-пайплайнами;

есть варианты модели порядка 1.6B параметров и готовые diffusers-чеки на Hugging Face.
NVIDIA
+2
Hugging Face
+2

Для VRAM это выглядит уже реалистичнее, чем многие более тяжёлые генеративные модели. Плюс у SANA есть линия развития SANA-Sprint, где упор уже на сверхбыструю генерацию в 1–4 шага, и там прямо упоминается интеграция с ControlNet для интерактивного управления. Это не делает её автоматически лучшей для твоей задачи, но показывает, что экосистема у неё не совсем “голый text-to-image”.
NVIDIA
+1

Если честно по-практически, то мой вывод такой:

Если вопрос “есть ли у NVIDIA что-то современное и интереснее простого SD?” — да, SANA это как раз такой кандидат.
NVIDIA
+1

Если вопрос “взял бы я SANA именно под ShadowGEN v2?” — скорее как экспериментальный вариант, но не как самый прямой и безопасный выбор.
Причина в том, что твоя задача не про свободную генерацию картинки по тексту, а про строгую управляемую генерацию физически правдоподобной тени от уже заданного объекта. Для этого удобнее архитектуры, где conditioning на изображение/маску является центральной частью задачи, а не надстройкой.

Я бы расставил так:

SANA — интересный вариант, если хочется современный backbone от NVIDIA и готовность повозиться с архитектурой.

SD3/FLUX-подобный backbone + ControlNet/IP-Adapter/кастомный conditioning — обычно практичнее по инструментам и чужому опыту.

Кастомная conditional diffusion, которая генерит не финальную картинку, а только shadow map — для твоей задачи это вообще может оказаться самым умным решением.

Вот это особенно важно: для ShadowGEN я бы не заставлял большую модель генерить сразу весь кадр “объект на белом фоне с тенью”. Я бы делал так:

вход: RGB объекта + маска + опционально depth/normal cue;

выход: только тень (shadow alpha / shadow RGB / soft shadow map);

потом обычный композитинг на белый фон.

Так ты резко упрощаешь задачу, уменьшаешь требования к данным и VRAM, и получаешь модель, которая учится именно физике тени, а не всему изображению сразу.

Итого по SANA:

да, это, вероятно, та самая модель “на z…”, которую ты вспоминал, только это SANA, а не на z;
NVIDIA
+1

да, она современная и реально интересная;
NVIDIA
+1

но под твою задачу я бы рассматривал её только если ты готов строить кастомный conditional pipeline, а не надеешься просто “дообучить как SD и поехать”.

Если хочешь, следующим сообщением я могу уже без воды собрать тебе конкретный shortlist из 3 моделей под твои ограничения:

вариант под 11 GB,

вариант под 24 GB / 4090,

и отдельно вариант “самый умный для теней, а не самый модный”.

## Turn 5 - User

Я наверное думал про z-image

## Turn 6 - Assistant

Проверю, что именно сейчас называют Z-Image, и есть ли у NVIDIA такая модель или это другой проект с похожим названием. Заодно отсею путаницу между названием модели и названием сервиса.

Да — похоже, ты вспоминал именно Z-Image. Но это не модель NVIDIA. Это open-weight семейство моделей от Tongyi/Alibaba, а NVIDIA лишь отдельно показывает и оптимизирует запуск Z-Image и Z-Image-Turbo на RTX/ComfyUI в своих материалах.
GitHub
+2
NVIDIA Developer
+2

Что это такое по сути:

Z-Image — базовая генеративная модель семейства;

архитектура описана как Scalable Single-Stream Diffusion Transformer (S3-DiT);

размер базовой модели — около 6B параметров;

у семейства есть варианты Z-Image-Turbo и Z-Image-Edit.
Hugging Face
+3
arXiv
+3
Hugging Face
+3

Почему ты мог связать её с NVIDIA:

NVIDIA в январе 2026 писала, что для RTX и ComfyUI доступны оптимизированные чекпойнты, среди которых есть Qwen-Image и Z-Image;

в их блогах Z-Image фигурирует рядом с FLUX и LTX-2 как модель, которую можно запускать на RTX-ПК.
NVIDIA Developer
+1

Теперь самое важное для твоего проекта.

Подходит ли Z-Image под ShadowGEN

Как база для эксперимента — да. Как самый прямой выбор — не факт.
Причина простая: публично Z-Image подаётся прежде всего как foundation image generation model, а не как специализированная модель для задачи вида RGB объекта + маска + контролируемая тень. Хотя у семейства есть Z-Image-Edit, что уже ближе к редактированию, это всё равно не означает, что у неё из коробки есть удобный и зрелый пайплайн именно под твой conditional setup.
arXiv
+2
Hugging Face
+2

Что у неё хорошего

Для твоих ограничений у Z-Image есть реальный плюс:

модель сделана с упором на эффективность;

авторы заявляют совместимость с consumer-grade hardware;

у Z-Image-Turbo прямо заявлена работа на железе <16 GB VRAM для инференса.
arXiv
+1

Но тут важно не перепутать:

инференс в <16 GB VRAM — это одно;

дообучение под твою задачу — совсем другое.
Для fine-tuning 6B diffusion-transformer уже может быть не таким приятным, как звучит по маркетинговому описанию. Это особенно актуально, если ты хочешь не просто prompt-following, а жёсткое conditioned generation по изображению и маске. Это уже мой инженерный вывод по тому, как такие модели обычно используются.

Мой честный вывод по Z-Image

Если выбирать для твоей задачи:

Хочешь попробовать современный “модный” backbone — Z-Image интересен.

Хочешь быстрее дойти до рабочего результата для теней — я бы всё же осторожнее смотрел на более прикладной conditional pipeline, а не на general-purpose image foundation model.

Для ShadowGEN я бы всё равно ставил выше такой подход:

не генерить весь финальный кадр;

генерить только shadow map / alpha shadow;

потом делать композитинг на белый фон.

Для этого тебе может быть полезнее не самая громкая модель, а та, которую проще переучить как image-conditioned diffusion под конкретный mapping. Это часто выгоднее и по качеству, и по VRAM.

Если коротко:

Z-Image — да, существует, современная, интересная;
arXiv
+1

не NVIDIA, а Tongyi/Alibaba;
GitHub
+1

NVIDIA её продвигает/ускоряет на RTX, отсюда и ассоциация;
NVIDIA Developer
+1

под твою задачу я бы рассматривал её как экспериментальный кандидат, а не очевидный первый выбор.

Могу следующим сообщением собрать тебе уже очень прикладной шортлист: 3 конкретные модели/архитектуры под ShadowGEN, отдельно для 11 GB, 24 GB, и вариант “лучший именно для теней”.

## Turn 7 - User

Обучать я могу на 4090, но инфнрентить на 1080ti.
Какую модель оптимально выбрать в 2к26

## Turn 8 - Assistant

Сейчас сведу это к практическому выбору именно под твою связку: учить на 4090, а крутить потом на 1080 Ti. Тут важна не только “крутость” модели, а ещё размер, скорость и насколько больно ей жить без современных тензорных фишек.

Если обучать на 4090, а инференсить на 1080 Ti 11 GB, то я бы сказал так:

Самый разумный выбор для продукта

Не FLUX, не Z-Image, не SD3.x.
Оптимально — компактная conditional latent diffusion под твою задачу, которая генерит только тень, а не весь кадр.

А если ты хочешь именно готовый современный backbone, тогда лучший кандидат из “новых” — SANA-0.6B. Но это уже второй выбор, не первый.

Почему я так считаю
Что сразу отпадает

FLUX.2 [klein] 4B официально заявлен как модель, которой нужно около 13 GB VRAM, то есть под 1080 Ti 11 GB он уже мимо.
docs.bfl.ml
+1

Z-Image / Z-Image-Turbo позиционируется как модель, которая “комфортно” работает на 16 GB VRAM consumer devices, то есть для 1080 Ti это тоже уже неудачный выбор как базовый рабочий вариант.
GitHub
+1

Stable Diffusion 3 Medium в публичных обсуждениях Stability/Diffusers часто упирается в существенно более высокий memory footprint; для локального комфортного запуска это уже совсем не тот класс модели, который хочется тащить на 1080 Ti. Stability позиционирует SD3/3.5 Medium как consumer-friendly, но на старой Pascal-карте без современных ускорителей это всё равно не выглядит оптимально.
Stability AI
+2
Stability AI
+2

Главная проблема именно 1080 Ti

GTX 1080 Ti — это Pascal, 11 GB GDDR5X, compute capability 6.1. У неё нет современных Tensor Cores, а FP16 на Pascal есть, но не является fast path. То есть даже если модель “влезает”, это не значит, что она будет приятной по скорости.
NVIDIA
+2
NVIDIA Developer
+2

Поэтому в твоём случае надо выбирать не “самую модную диффузионку”, а ту, которая:

реально влезает в 11 GB,

не превращает инференс в боль на Pascal,

легко кондиционируется картинкой и маской,

дообучается на 4090 без извращений.

Мой итоговый выбор
Вариант 1 — лучший для ShadowGEN
Кастомная latent diffusion / conditional diffusion, которая предсказывает только shadow map

То есть не “нарисуй целую картинку”, а:

вход

RGB объекта

маска

опционально depth / normal cue

выход

shadow alpha map

или soft shadow RGB/alpha

а потом ты уже сам делаешь:

белый фон

композитинг объекта

наложение тени

Почему это лучший путь

Потому что твоя задача — узкая и физичная, а не general image generation.
Генерировать только тень:

проще по данным,

проще по loss,

меньше артефактов,

легче держать консистентность края объекта,

заметно дешевле по VRAM и latency.

Это уже не привязано к маркетинговому названию конкретной foundation model — это правильный формат задачи.

Что брать как размер/класс модели

По размеру я бы целился в SD1.5-class latent UNet или даже немного компактнее.
Причина простая: экосистема для conditional diffusion и ControlNet-подобных входов огромная, а inference на 1080 Ti будет реалистичным. ControlNet как подход вообще как раз создан для добавления внешних условий к большой frozen diffusion-модели.
Hugging Face
+2
Hugging Face
+2

Мой вердикт

Если цель — сделать рабочий продукт, а не исследовательский красивый эксперимент, я бы выбрал именно это.

Вариант 2 — лучший “современный backbone”, если хочется что-то новое
SANA-0.6B

Из современных моделей это самый интересный кандидат под твою железку.

У SANA официальный репозиторий и модельная линейка с упором на эффективность; в репозиториях/документации и производных сборках указывают, что:

0.6B требует около 9 GB VRAM,

1.6B требует около 12 GB VRAM,

позже доступны и ещё более лёгкие варианты через квантизацию.
GitHub
+2
GitHub
+2

То есть:

SANA-0.6B — ещё реально под 1080 Ti,

SANA-1.6B — уже на грани или мимо,

при этом SANA официально имеет training/inference pipeline и поддержку LoRA fine-tuning.
GitHub
+2
GitHub
+2

Плюсы SANA

современный efficient diffusion backbone;

реально компактнее FLUX-класса;

официально есть LoRA/fine-tune story;

лучше смотрится в 2026, чем просто “снова SD1.5”.

Минусы для тебя

SANA всё же в первую очередь text-to-image family, а не специальная image-conditioned shadow model;
nvlabs.github.io
+1

тебе всё равно придётся строить кастомное conditioning на image + mask, а возможно ещё на depth;

экосистема под узкую задачу редактирования/маскирования у SD-семейства всё ещё практичнее.

Мой вердикт

Если очень хочется “что-то современное в 2026”, бери SANA-0.6B.
Но я бы использовал её не для генерации финальной картинки, а всё равно для генерации shadow layer.

Вариант 3 — что я бы не советовал как основной путь
FLUX / Z-Image / крупные SD3.x

Не потому что они плохие, а потому что они не совпадают с твоим deployment target.

FLUX.2 [klein] — уже хочет примерно 13 GB VRAM, то есть выше потолка 1080 Ti.
Black Forest Labs
+1

Z-Image-Turbo — скорее история про 16 GB consumer VRAM, а не про 11 GB Pascal.
GitHub
+1

SD3 / SD3.5 — даже если местами “consumer”, на практике под 1080 Ti это уже плохой баланс размера, скорости и возни.
Stability AI
+2
Stability AI
+2

Что бы я выбрал лично на твоём месте
Если нужен лучший инженерный выбор

Custom shadow-only latent diffusion на базе SD1.5-sized UNet

Почему:

проще обучить на 4090;

легче стабильно инференсить на 1080 Ti;

легко сделать conditioning на маску;

можно добавить second branch для depth / light direction;

модель будет решать ровно твою задачу, а не пытаться быть универсальным генератором всего на свете.

Если нужен лучший “современный open model” под 1080 Ti

SANA-0.6B

Но именно как:

backbone,

с кастомным conditioning,

желательно под генерацию тени, а не всего изображения.

Моя короткая рекомендация в одну строку

Оптимально в 2026 для твоей связки 4090 → 1080 Ti:

Лучший практический выбор: кастомная shadow-only latent diffusion уровня SD1.5-sized UNet.

Лучший современный backbone: SANA-0.6B.

Не брать как основной путь: FLUX.2 klein, Z-Image, SD3.x — слишком тяжёлые или просто невыгодные под 1080 Ti.
Hugging Face
+3
GitHub
+3
Black Forest Labs
+3

Если хочешь, я следующим сообщением могу уже собрать тебе конкретную архитектуру по блокам:
что подать на вход, что именно предсказывать, какие loss’ы взять и как уместить обучение в 4090, чтобы потом это нормально ехало на 1080 Ti.

## Turn 9 - User

получается лучше всего всё равно дообечать сд 1.5 ?

## Turn 10 - Assistant

Сведу это к честному ответу без маркетинга: не “самая новая” модель, а что даст лучший результат именно под твою задачу и железо. Заодно отделю “дообучать SD 1.5” от “использовать SD 1.5 как размерный класс модели”, потому что это не совсем одно и то же.

Да, в твоих условиях это, скорее всего, лучший базовый выбор — но с важной поправкой:

лучше не “дообучать именно обычный SD 1.5 как text-to-image”, а брать SD 1.5-класс как лёгкий latent backbone для кастомной conditional модели под тени.

Это чуть разные вещи.

Почему я так считаю:

1. Под 1080 Ti тебе важнее размер и скорость, чем модность модели.
FLUX.2 [klein] просит примерно 13 GB VRAM, то есть уже выше потолка 1080 Ti 11 GB.
docs.bfl.ml
+1

2. SANA-0.6B выглядит интереснее “по-современному”, но это скорее запасной вариант, не самый практичный.
У SANA 0.6B заявлено около 9 GB VRAM для инференса, а у 1.6B — около 12 GB. То есть 0.6B уже ближе к твоему лимиту, но это всё ещё более новый и менее “родной” стек для узкой image+mask задачи, чем SD-семейство.
GitHub

3. Z-Image для твоего деплоя слишком тяжёлый как основной путь.
Семейство Z-Image — это 6B-параметрическая линия, а по GitHub issue у Turbo обычно называют 16–20 GB VRAM для инференса. На 1080 Ti это уже плохая ставка.
GitHub
+1

4. У SD 1.5-класса до сих пор лучшая практическая экосистема для conditioning.
ControlNet как раз создан для того, чтобы добавлять к pretrained diffusion-модели пространственные условия — depth, segmentation, edges и другие карты. Это очень близко к твоей постановке “RGB объекта + маска + возможно depth → тень”.
Hugging Face
+1

И вот тут главное:

Что я бы делал на практике
Не так:

взять vanilla SD 1.5

доучить его “рисовать объект на белом фоне с тенью”

А так:

взять SD 1.5-sized latent UNet

сделать кастомный conditional diffusion

подавать на вход:

RGB объекта

бинарную маску

опционально depth / light direction

предсказывать:

shadow map / alpha shadow, а не весь кадр

Потом уже отдельно:

белый фон

композитинг объекта

наложение тени

Это почти наверняка будет лучше для ShadowGEN, чем дообучать большую general-purpose модель на полный кадр.

Мой честный вывод

Если вопрос звучит буквально так:

«Лучше всего ли мне дообучать SD 1.5?»

Ответ: да, по духу — да.
Но я бы сформулировал точнее:

Лучше всего брать архитектуру уровня SD 1.5 по размеру и latent-представлению, но обучать её как специализированную conditional shadow model.

То есть:

SD 1.5 — да, как размерный класс и инженерная база

не обязательно SD 1.5 — как финальная “идеология text-to-image”

главный фокус — shadow-only prediction

Если совсем коротко

Для твоей связки 4090 train → 1080 Ti infer я бы ранжировал так:

1. Лучший выбор:
SD 1.5-class latent conditional diffusion, которая генерит только тень.

2. Второй выбор:
SANA-0.6B, если хочется более современный backbone и готовность повозиться.
GitHub

3. Не как основной путь:
FLUX, Z-Image, SD3.x — слишком тяжёлые или просто невыгодные под 1080 Ti.
docs.bfl.ml
+2
GitHub
+2

Если хочешь, я могу дальше уже очень предметно расписать архитектуру именно под ShadowGEN: какие входы, какой target, какие loss’ы и как это тренировать на 4090 без лишней боли.

## Turn 11 - User

навернео всё равно лучше дообечать чем учить с нуля.
тень я хочу рисуовать у сложных объектов снятый не сверху и не фронтально а под углом. то есть тут важна будет семантика

## Turn 12 - Assistant

Сейчас уточню актуальные подходы именно для случая, где тень зависит не только от маски, но и от понимания формы объекта под углом. Тут уже важно разделить “дообучить” и “что именно дообучать”.

Да, для твоего случая дообучать pretrained-модель почти наверняка лучше, чем учить всё с нуля.

И причина как раз в том, что ты написал: для тени у сложного объекта под углом важна семантика, а не только силуэт. Модель должна понимать, что перед ней, условно, не “абстрактная маска”, а кружка с ручкой, кроссовок, стул, игрушка, прозрачная бутылка, объект с выступами, полостями и нависающими частями. Это знание как раз и живёт в больших pretrained diffusion backbones. ControlNet изначально и задумывался так: взять уже сильную pretrained text-to-image модель и добавить к ней пространственные условия, не теряя мощный визуальный prior.
Hugging Face
+1

То есть твоя интуиция верная: если объект снят не сверху и не фронтально, одной маски уже мало. Маска даёт контур, но не даёт:

какие части ближе/дальше,

где нависание,

где форма выпуклая или вогнутая,

где прозрачность или тонкие детали,

как примерно “лежит” объект в 3D.
Для этого обычно добавляют depth / normal / geometry cues как дополнительные условия. У ControlNet есть стандартные режимы conditioning по depth и normal map, и это как раз тот тип сигналов, который помогает модели учитывать форму, а не только 2D-силуэт.
Hugging Face
+2
Hugging Face
+2

Отсюда практический вывод:

Да, дообучать лучше. Но дообучать не “как попало”

Я бы не делал ставку на схему:

“берём vanilla SD 1.5”

“скармливаем пары картинка→картинка”

“надеемся, что оно само поймёт физику тени”

Я бы делал так:

pretrained backbone уровня SD 1.5

поверх него conditional branch

условия:

RGB объекта

binary mask

depth map

возможно normal map

опционально параметр света

target:

shadow map, а не весь финальный кадр

Это лучше, потому что pretrained backbone уже содержит богатую визуальную семантику, а твоя донастройка учит его привязывать эту семантику к задаче тени. ControlNet как раз хорош тем, что позволяет учить такую условную привязку даже на относительно небольших датасетах, сохраняя силу базовой модели.
Hugging Face
+1

Почему “с нуля” тут хуже

Если учить с нуля, тебе надо одновременно научить модель:

распознавать классы и типы объектов,

понимать их форму по одной картинке,

понимать связь формы и тени,

ещё и аккуратно рисовать мягкие полутени.

Это слишком много для одной узкой выборки, если у тебя не гигантский датасет. А pretrained diffusion уже приходит с сильным prior на естественные формы и изображения. В обзорах controllable diffusion как раз отмечается, что современные методы опираются на pretrained foundation models и усиливают их внешними условиями, а не переизобретают всё с нуля.
arXiv
+1

Но семантики одной тоже мало

Тут тонкий момент:
семантика нужна, но одной семантики недостаточно.

Для тени под углом тебе нужны два слоя информации:

1. Семантика
Чтобы модель понимала тип формы и типичные 3D-структуры объекта.

2. Геометрические подсказки
Чтобы она не “фантазировала” форму слишком свободно.
Именно поэтому я бы обязательно добавлял хотя бы depth, а ещё лучше пробовал depth + normals. Для похожих задач relighting и controllable image editing диффузионные методы уже используют такие сигналы, чтобы лучше держать связь между освещением и формой.
arXiv
+1

Что бы я выбрал конкретно

Для твоего железа и цели я бы сказал так:

Лучший путь:
дообучать SD 1.5-class backbone, но не как обычный text-to-image, а как conditional shadow diffusion.

Оптимальная постановка:

вход: object RGB + mask + depth (+ normals, если получится)

выход: shadow alpha / shadow map

Потом:

белый фон

композитинг объекта

блюр/softness можно частично доверить модели, частично постпроцессу

Почему именно SD 1.5-class, а не что-то новее

Не потому что SD 1.5 “самая умная”, а потому что в твоём сценарии она даёт лучший баланс:

влезает в инференс на 1080 Ti,

имеет зрелую экосистему conditioning,

её проще кастомизировать,

и pretrained семантики там уже достаточно, чтобы не начинать с нуля.
А более новые большие модели в твоём случае чаще проигрывают по deploy-цене, чем выигрывают по качеству.
Hugging Face
+2
Hugging Face
+2

Итого:

Да — дообучать лучше, чем учить с нуля.
Да — если тень должна быть правдоподобной у сложных объектов под углом, тебе нужен pretrained semantic backbone.
И да — оптимальнее всего не просто “дообучать SD 1.5”, а делать на её базе conditional модель с geometry conditioning.

Следующий самый полезный шаг — выбрать уже точную постановку target’а:
генерить shadow map, alpha-тень или сразу RGB-тень.

## Turn 13 - User

тень я хочу что бы была с цветными рефлексами

## Turn 14 - Assistant

Тогда задача уже не просто “нарисовать тень”, а почти shadow + contact lighting + reflected color bleed. Это усиливает аргумент в пользу дообучения сильного бейзлайна и меняет target: чистая маска тени уже может быть слишком бедной.

Да — тогда чистую alpha-тень я бы уже не делал основным target’ом.

Если ты хочешь цветные рефлексы, то модель должна предсказывать не просто “где темнее”, а часть светотранспортa: мягкую тень, контактное затемнение и color bleed от самого объекта/окружения. Это уже ближе к задачам relighting / intrinsic editing, где современные diffusion-методы работают не только с маской, а с геометрией и освещением. Например, SpotLight использует intrinsic-карты вроде depth, normals и albedo для управления relighting, а работы по intrinsic-space editing отдельно подчеркивают, что глобальные световые эффекты потом восстанавливаются автоматически.
arXiv
+1

Из этого следует важная смена постановки:

Что лучше предсказывать

Не shadow_alpha, а один из двух вариантов:

Вариант А — лучший для тебя

RGB shadow / illumination residual

выход модели: 3 канала RGB с отрицательной и положительной поправкой света

то есть не только затемнение, но и слабый цветной вклад

Практически это можно мыслить как:

где-то модель затемняет,

где-то добавляет тёплый/холодный оттенок,

где-то рисует мягкий рефлекс рядом с объектом.

Это ближе к тому, что ты хочешь визуально.

Вариант Б — более инженерный

Разделить на:

1 канал: shadow density / opacity

3 канала: reflected color map

Так удобнее дебажить, но сложнее пайплайн.

Что это меняет по выбору модели

Это ещё сильнее говорит в пользу дообучения pretrained backbone, а не обучения с нуля.
Потому что цветные рефлексы зависят от:

материала объекта,

локальной формы,

семантики,

цвета самого объекта,

того, как модель “понимает” освещение сцены.

У pretrained diffusion backbone уже есть мощный визуальный prior, и ControlNet как раз построен на идее: заморозить сильную базовую модель и доучить маленькую conditional-ветку под внешние карты, не переобучая всё с нуля. В документации Hugging Face это прямо так и описано.
Hugging Face
+1

Что я бы выбрал теперь ещё увереннее
Да: SD 1.5-class backbone

Но не как “рисуй финальную картинку”.

Лучше:

SD 1.5-sized latent conditional diffusion, которая получает:

RGB объекта

mask

depth

желательно normal map

опционально light direction / light color

и предсказывает:

RGB illumination layer
или

shadow + color-bleed decomposition.

Depth и normal conditioning для ControlNet уже давно используются как стандартные геометрические условия, и это как раз то, что тебе нужно для объектов под углом.
Hugging Face
+2
Hugging Face
+2

Самая важная мысль

Если нужна реалистичная тень с цветными рефлексами, то задача уже называется не “shadow generation”, а скорее:

object-ground light interaction

relighting for compositing

illumination harmonization

И тут правильный target — не бинарная тень, а локальный lighting residual.

Мой практический совет

Я бы делал первый рабочий вариант так:

Input

object RGB on transparent/isolated background

mask

estimated depth

optional normals

Output

4 канала:

RGB = цветной световой вклад/рефлекс

A = интенсивность/плотность эффекта

А финальный композит:

белый фон

объект

снизу/рядом lighting layer

Так ты сможешь получить и:

тёмную контактную тень,

мягкую основную тень,

цветной рефлекс у основания.

Итог

Да, с цветными рефлексами мой ответ становится ещё более определённым:

лучше не учить с нуля;
лучше не ограничиваться alpha-тенью;
лучше дообучать SD 1.5-класс как conditional relighting/shadow model.

То есть:

база: SD 1.5-sized latent backbone

conditioning: image + mask + depth + normals

target: RGB lighting/shadow residual, а не только mask shadow

Это, по-моему, уже почти оптимальная постановка под твой ShadowGEN.

Могу следующим сообщением набросать тебе уже конкретную архитектуру выходов и loss’ов под этот RGB+alpha lighting layer.

## Turn 15 - User

с архитектурой понятно. теперь надо подумать как сделать датасет. раньше для теней вида "сверху" я рендерил абятрактные формы с тенью в 3д. для ракурса сверху-сбоку так уже не получится. делать в 3д всевозможные формы преметов слишком трудоемко.  наверное буду проовать генерировать картинки с тенью более умными моделями а потом делать из них датасет. какой нужн рпзмер датасета? в идеале я бы ещё хотел иметь возможность задавать сторону в которую отбрасывает тень, то есть дадавать угол в градусах куда падает тень, чтобы потом можно было вращать. но непонятно как генерировать тень с нужным углом.

## Turn 16 - Assistant

Сейчас соберу практичные ориентиры по размеру датасета и посмотрю, как в свежих работах задают направление света/тени — через явный угол, light vector или через более удобное параметрическое описание.

Да, тут уже ключевой вопрос не в архитектуре, а в правильной разметке задачи и в устройстве датасета.

Мой вывод такой: полностью генерить датасет “умной моделью” я бы не делал. Лучше делать гибрид:

немного физически правдоподобных данных, где направление света известно точно;

много дешёвых синтетических/псевдоразмеченных данных;

и отдельно маленький ручной eval-набор, чтобы не начать учить модель на ошибки генератора. Это особенно важно, потому что свежие работы по relighting/shadow-editing тоже приходят к смеси small real + large synthetic, а не к “обучим на чисто сгенерённых картинках и всё”. LightLab прямо показывает, что небольшая реальная часть помогает убрать domain drift, а синтетика даёт масштаб и физическую управляемость.
arXiv
+1

Что я бы делал с датасетом
1. Не генерить сразу финальную картинку

Лучше хранить не только input -> final image, а разложить пример на слои:

object_rgb

object_mask

ground/background — у тебя это часто белый фон

depth или pseudo-depth

light params

target_lighting_layer

И вот target_lighting_layer я бы делал не бинарной тенью, а RGBA residual:

A — где и насколько сильный эффект,

RGB — цветной вклад, включая рефлексы.

Так ты не загоняешь модель в “рисуй всё с нуля”, а учишь именно взаимодействие объекта со светом.

2. Угол задавать не “в градусах тени”, а “параметрами источника света”

Это очень важный момент.

Не стоит метить датасет как:

“тень падает на 37°”.

Лучше метить как:

azimuth

elevation

distance

light_size

intensity

light_color

Потому что в свежей работе по controllable shadow generation именно так и делают: источник света задают в сферических координатах — полярный угол, азимут и радиус — и этим получают управляемые направление, мягкость и интенсивность тени.
arXiv

То есть для твоего интерфейса можно потом оставить “угол в градусах”, но внутри модели лучше использовать что-то вроде:

light_azimuth_deg
light_elevation_deg
light_distance
light_radius
light_intensity
light_rgb

А если хочешь компактнее для сети:

light_dir = (dx, dy, dz)
light_color = (r, g, b)
light_size
light_intensity

Это лучше, чем один угол, потому что один угол не различает, например:

низкий боковой свет,

высокий боковой свет,

почти фронтальный верхний свет.

Как получать данные, если не хочется моделить тысячи 3D-объектов вручную
Рабочий путь: 3 уровня данных
Уровень A — “якорные” физические данные

Небольшой, но чистый набор.

Не нужно вручную делать тысячи моделей. Можно взять:

готовые 3D-ассеты из Objaverse / ABO / ShapeNet-подобных источников,

отфильтровать категории, похожие на твои будущие объекты,

отрендерить только нужный тебе стиль: белый фон, заданный свет, несколько ракурсов.

В работе про controllable shadow generation использовали 9,922 3D meshes и рендерили тени при разных параметрах света; это показывает, что именно разнообразие геометрии даёт хорошую обобщающую способность.
arXiv
+1

Тебе не обязательно гнаться за 10k мешей. Для pet/project-уровня я бы начал намного скромнее:

300–1000 3D объектов

по 20–80 lighting-конфигураций на объект

по 2–6 ракурсов на объект

Это уже даёт от 12k до 480k примеров, в зависимости от сетки параметров. Это реалистично.

Уровень B — псевдореальные данные

Берёшь реальные фото вырезанных объектов и строишь псевдо-GT:

depth из монокулярной модели,

contact shadow / coarse cast shadow из простой геометрии,

потом можно прогонять через сильную generative relighting-модель как refinement,

но не использовать это как единственную истину.

Смысл: генеративная модель может помочь сделать слой красивее, но физическую привязку лучше брать не из неё, а из параметров света и геометрических подсказок.

Уровень C — маленький реальный контрольный набор

Очень советую сделать руками 200–1000 реальных пар:

один и тот же объект,

белый фон,

фиксированная камера,

свет с нескольких известных направлений,

желательно несколько цветовых температур.

Даже небольшая реальная часть очень полезна: LightLab обучали на смеси 600 реальных raw-пар плюс большая синтетика, и именно смесь дала лучший результат, чем один домен сам по себе.
arXiv
+1

Какой размер датасета нужен

Тут нет магического числа, но для твоего случая я бы ориентировался так.

Минимум, чтобы что-то заработало

20k–50k примеров
если:

задача узкая,

фон почти всегда белый,

выход — lighting/shadow layer,

conditioning хороший: mask + depth + light params.

Это нижняя граница для вменяемого первого результата. Для ориентира: в SGDiffusion-подобной линии реальный shadow dataset DESOBAv2 содержит 28,573 пар, и это уже считается “large-scale” относительно старого DESOBA.
arXiv

Нормально

100k–300k примеров
Это уже диапазон, где можно ждать устойчивости по разным категориям предметов и углам света.

Хорошо

300k+
Особенно если хочешь:

сложные формы,

прозрачные/полупрозрачные элементы,

цветные рефлексы,

стабильную управляемость углом.

Но тут важнее не число само по себе, а покрытие пространства:

формы,

материалов,

ракурсов,

высоты света,

цветовой температуры,

расстояния до плоскости.

Я бы честно сказал так:
лучше 80k хорошо параметризованных примеров, чем 500k “красивых, но непонятно откуда и с какими ошибками”.

Как задать управление направлением тени

Самая практичная схема:

В датасете хранить:

azimuth — направление вокруг объекта, 0…360°

elevation — высота света над горизонтом

size — размер источника

intensity

color_rgb или color_temp

object_to_plane_offset — насколько объект “стоит” над плоскостью

при желании camera_elevation / camera_azimuth

В модели подавать:

либо числовой вектор,

либо sinusoidal encoding:

sin(azimuth), cos(azimuth),
sin(elevation), cos(elevation),
log(distance),
log(size),
intensity,
light_r, light_g, light_b

Это лучше простого числа в градусах, потому что угол циклический.

Но как генерировать тень с нужным углом, если нет 3D?

Полностью надёжно — никак.
Если нет хотя бы приближённой геометрии, задача остаётся неоднозначной.

Поэтому я бы не пытался заставить генератор “сам угадывать” угол тени только из текста или из одного числа. Лучше дать ему геометрический prior.

Практичный компромисс

Для каждого реального объекта:

получить mask;

получить monocular depth;

из depth вычислить грубую LGI / visibility-like map или хотя бы projected occlusion prior;

на основе заданного light vector получить coarse shadow proposal;

diffusion-модель уже улучшает форму, мягкость, цвет и рефлексы.

Это очень близко к направлению свежих работ. Например, в VSDiffusion лучшие результаты даёт комбинация Light + Depth, а не что-то одно; авторы прямо пишут, что свет задаёт геометрию освещения, а depth даёт структуру сцены.
arXiv

То есть для тебя оптимальный путь такой:

не генерить тень с нуля из воздуха;

сначала строить coarse physically-guided shadow prior из depth + light direction;

потом diffusion дорабатывает:

мягкость,

контакт,

цветной bleed,

сложные формы.

Мой конкретный рецепт для ShadowGEN
Что бы я сделал в v1 датасета

Собрал бы датасет из трёх частей:

1. Synthetic core

500–1000 готовых 3D объектов

24 azimuth-угла

5 elevation-углов

3 размера источника

3 интенсивности

3 цвета света

2–4 камеры

Это уже огромный комбинаторный объём, так что не надо полный декартов продукт. Лучше случайная выборка 80k–200k комбинаций.

2. Real calibration set

100–300 реальных объектов

8–16 направлений света

2–3 цвета света

1–3 ракурса камеры

Получишь примерно 3k–10k реальных примеров.

3. Hard cases
Отдельно добить:

объекты с дырками,

тонкие ножки,

ручки кружек,

обувь,

стекло/полупрозрачность,

глянцевые материалы,

тёплые/холодные рефлексы.

Именно hard cases потом больше всего влияют на субъективное качество.

Стоит ли использовать “умные модели” для генерации датасета

Да, но только как вспомогательный инструмент:

для proposal generation,

для style refinement,

для augmentations,

для bootstrapping там, где нет GT.

Но я бы не строил весь train set на outputs другой diffusion-модели. Иначе твоя модель начнёт копировать её ошибки: неправильную геометрию, слишком “красивую, но не физичную” тень, нестабильные рефлексы.

Самый безопасный вариант:

источник истины для направления и грубой формы — физика/геометрия;

источник истины для фотореалистичности — смесь синтетики, реальных примеров и немного генеративного refinement.

Итог в одной строке

Для тебя я бы целился в:

80k–200k synthetic/pseudo-synthetic

3k–10k real calibration

управление не “углом тени”, а light vector / spherical light params

и обязательно промежуточный coarse shadow prior из depth, а не попытку сразу учить diffusion угадывать всё с нуля.
arXiv
+3
arXiv
+3
arXiv
+3

Могу следующим сообщением предложить тебе уже совсем прикладную схему CSV/JSON-формата для такого датасета и как именно кодировать azimuth/elevation/color/intensity для обучения.

## Turn 17 - User

я как-то не думал что на эту задачу есть статьи. давай поищи вообще какие есть подходы к решению, какие сота в 2026, на что имеет смысл лопираться мне в моих ограничениях - 24гб на трейн, 11 гб на инференс, требуется высокое качество как на студийных фотосессиях, с рефлексами, скорость обработки до 5 сек на 1080ти

## Turn 18 - Assistant

Error loading app
Failed to fetch template
Retry

## Turn 19 - User

наверное мне ещё надо будет нужна доп модели. 1 по фотографии предсказывать параметры угла съемки (наклон пола, угол обзора, может ещё что-то) это потребуется для того чтобы более точно генерить тень
2 по фотографии объекта на белом с тенью предсказывать пареметры освещения, азимут, жесткость и всё такое. это надо будет чтобы размечать мои качественные фотографии и ли генерации для файнтюна модели обученной на грубой синтетике

## Turn 20 - Assistant

Сейчас соберу это в практичную схему: отдельно посмотрю, что есть по теням/композитингу, что по оценке света из фото, и что по геометрии сцены из одного изображения. Потом сведу к тому, какие допмодели тебе реально нужны при твоих лимитах.

Да — эти две допмодели выглядят не как “nice to have”, а как почти обязательные, если ты хочешь не просто “красивую тень”, а стабильный студийный результат с хорошей геометрией, мягкостью, рефлексами и без плавающих артефактов.

По тому, что есть в литературе на 2024–2026, тренд очень явный: лучшие результаты уже не пытаются решать всё одной сеткой “RGB → готовая картинка”. Они раскладывают задачу на геометрию + свет + генерацию/рендер. Именно в эту сторону идут и работы по shadow compositing, и object insertion, и relighting. В 2024–2026 сильные подходы почти всегда добавляют явные priors: глубину/геометрию, оценку освещения, иногда inverse rendering, а уже потом генеративную часть. Это видно и по DiffusionLight для оценки освещения, и по DIPIR для вставки объектов, и по MetaShadow, и по совсем свежей работе 2026 года с Light-Geometry Interaction maps, где авторы прямо показывают, что без геометрического priора генерация даёт “floating shadows” и физически странные результаты.
papers.cool
+4
diffusionlight.github.io
+4
NVIDIA
+4

Для твоего кейса я бы это сформулировал так:

Что имеет смысл добавить как отдельные модели
1) Модель геометрии кадра / камеры

Твоя идея с предсказанием наклона пола, угла обзора и похожих параметров — правильная.

Но я бы не делал выходом “сырые фотографические параметры” в духе просто pitch / yaw / FOV. Для тени важнее предсказать:

mask опорной поверхности: где вообще лежит пол/стол/плоскость

нормаль опорной поверхности в камере

контактную линию / точку опоры объекта

horizon / vanishing geometry, если сцена не фронтальная

относительную глубину вокруг объекта

по возможности scale proxy: насколько объект “оторван” от камеры и плоскости

То есть лучше делать не “camera estimator”, а scene-geometry estimator for shadowing.

Почему так: тень определяется не абстрактным FOV, а тем, куда проектируется объект на реальную плоскость. Даже грубая, но стабильная оценка плоскости и глубины полезнее, чем попытка напрямую регрессировать красивый угол камеры. Новые shadow-подходы как раз показывают, что монокулярный 2.5D-приор уже сильно помогает генерации тени.
papers.cool
+2
arXiv.gg
+2

Для тебя практичнее всего:

взять сильную готовую depth-модель как backbone/teacher, например семейство уровня Depth Anything V2;

сверху маленькую голову, которая предсказывает:

plane mask

plane normal

contact footprint

optional camera tilt / horizon line

Depth Anything V2 особенно интересен тем, что он заметно эффективнее многих diffusion-based depth моделей и даёт более детальные depth maps, а тебе нужна именно скорость и деталь на границе объекта.
arXiv.gg

2) Модель оценки освещения по фото объекта на белом фоне с тенью

Это тоже очень здравая идея. Причём я бы делал выход не “полный HDR envmap”, а параметризованный studio-light descriptor.

Для твоего домена полный envmap часто избыточен. Тебе полезнее предсказывать:

азимут основного источника

elevation

размер / softness источника

relative intensity

fill ratio

color temperature / tint

наличие второго слабого источника

strength of ground bounce / reflex

возможно shadow density как отдельно управляемую величину

То есть не general illumination estimation for the wild, а studio-light estimation in a compressed latent parameterization.

Это очень хорошо сочетается с твоей задачей разметки данных:

берёшь хорошие реальные студийные фото на белом фоне,

прогоняешь через light estimator,

получаешь приближённые параметры,

потом либо руками быстро правишь только сложные случаи,

либо используешь их как soft labels для дообучения shadow/render части.

Здесь полезны две линии работ:

классическая оценка света из одного изображения;

более свежие diffusion-based light probe methods, где освещение восстанавливают через “виртуальный chrome ball”.

DiffusionLight показал сильную идею: вместо прямой регрессии envmap задача сводится к “дорисуй хромированный шар в сцену”, и из него уже восстанавливается освещение. Это особенно ценно как teacher/annotator, даже если сам метод тяжёлый для финального продакшена. Turbo-версия 2025 дополнительно показала, что это можно ускорять.
diffusionlight.github.io
+1

Что я бы сделал в твоей архитектуре

Не одна end-to-end модель, а 4 блока:

A. Subject extraction

твой сегментатор / matting

плюс contact mask снизу объекта

B. Geometry branch

monocular depth / local 2.5D

plane normal + floor/table mask

horizon / tilt

optional support-point predictor

C. Lighting branch

studio-light parameters

возможно грубая reflectance class: matte / semi-gloss / glossy / transparent

D. Shadow-relight renderer

сначала быстрый физический rough render:

hard occlusion

softening by source size

contact shadow

rough ambient bounce

потом маленькая refinement-сеть:

улучшает penumbra

добавляет рефлексы

чинит прозрачности/полупрозрачности

сглаживает композит

Именно такая “physics first, neural refinement second” схема у тебя лучше всего ляжет в лимиты 24 GB train / 11 GB inference. Полностью diffusion-based генератор под 1080 Ti и лимит до 5 секунд выглядит слишком рискованно для продакшена, особенно если нужен 1080p и стабильность на потоке. А вот физически-грубый рендер + компактный refinement-UNet — это уже реалистично. И свежие работы фактически движутся туда же: либо inverse rendering + diffusion guidance, либо geometry-aware generation.
NVIDIA
+2
papers.cool
+2

Какие подходы выглядят самыми полезными для тебя
Полезно опираться

1. Geometry-aware shadow generation
Это, пожалуй, самый близкий тебе класс. Главная мысль: тень нельзя надёжно выучить только по RGB. Нужен хотя бы depth/plane prior. Очень хорошо совпадает с твоей идеей про предсказание наклона пола и ракурса.
papers.cool
+1

2. Inverse rendering / illumination estimation
Не обязательно как финальный pipeline, но как источник supervision и разметки — очень ценно. DIPIR особенно важен концептуально: реалистичная вставка требует не только генерации, но и восстановления параметров сцены и tone mapping.
NVIDIA
+1

3. Object-centered shadow synthesis
MetaShadow важен как признак того, что сообщество перешло от “shadow как побочный эффект” к “shadow как отдельный управляемый объект”. Для твоего проекта это аргумент в пользу явного shadow head, а не только финального RGB output.
Adobe Research
+1

4. Lighting estimation via chrome-ball / probe formulation
Очень хорошая опора именно для твоей второй допмодели. Даже если не брать метод целиком, сама формулировка задачи сильная.
diffusionlight.github.io
+1

На что не стоит слишком опираться как на основу продукта

Полный диффузионный end-to-end compositing.
Качество может быть высоким, но:

сложно держать детерминированность

тяжело уложиться в 5 сек на 1080 Ti

трудно гарантировать стабильную геометрию тени

VRAM/latency будут неприятными

Я бы использовал diffusion максимум как:

teacher для разметки

offline data generation

refinement for hard cases

отдельный research branch, но не продовый backbone

Что считать “SOTA” в 2026 именно для твоей задачи

Если по-честному, у этой задачи нет одного общепризнанного “лидера” как у обычной segmentation benchmark-задачи. Есть несколько соседних направлений:

shadow-specific compositing/synthesis: MetaShadow и geometry-aware shadow papers
Adobe Research
+2
Hugging Face
+2

joint shadow + relighting: свежая 2026 работа с LGI maps выглядит очень сильной концептуально, потому что она объединяет тень и освещение, а не учит их отдельно
papers.cool

photorealistic insertion / inverse rendering: DIPIR — очень сильная опора по идеологии, но тяжеловат для твоих ограничений как есть
NVIDIA
+1

lighting estimation: DiffusionLight и его ускоренные варианты очень заметны как practical SOTA-ish family для single-image lighting recovery in the wild
diffusionlight.github.io
+1

То есть “SOTA 2026” для тебя — это не одна модель, а модульный hybrid pipeline:

geometry prior

lighting prior

physically grounded shadow render

neural refinement

Что я бы рекомендовал лично тебе при твоих лимитах
Минимально жизнеспособный сильный стек

Инференс на 1080 Ti до 5 секунд:

сегментация/маттинг: уже есть или лёгкая специализированная модель

depth/geometry: готовая быстрая depth-модель небольшого размера

plane/light estimators: маленькие CNN/ViT головы

shadow renderer: differentiable but lightweight

refinement net: UNet / SR-like residual net на 512–768 px, потом upscale/composite to full-res

Это реалистичнее, чем большой diffusion.

Что обучать отдельно

Geometry head
Вход: фото без фона или с белым фоном, плюс optionally original context
Выход:

floor normal

contact map

horizon/tilt

shadow projection basis

Light head
Вход: объект на белом с тенью
Выход:

azimuth

elevation

softness

intensity

fill

color temperature

bounce strength

Material/style head
Небольшая, но полезная:

matte / glossy / transparent / metallic-like

maybe roughness bin

Она нужна, чтобы решать, сколько рефлекса и какого типа может быть у объекта. Для “студийности” это очень важно.

Как обучать

Я бы делал так:

Этап 1. Синтетика

рендеришь много объектов на белой/светлой плоскости

варьируешь:

свет

softness

camera tilt

plane orientation

object elevation

materials

bounce

получаешь идеальные GT для shadow mask, alpha shadow, contact shadow, depth, normals, light params

Этап 2. Реальные студийные фото

light estimator на них делает pseudo-labels

где надо, вручную правишь только малую часть

refinement сеть доучиваешь уже на real+synthetic mix

Этап 3. Hard-negative mining

прозрачные бутылки

блестящие предметы

мех/волосы/бахрома

тонкие ножки, проволока, ручки кружек

полупрозрачные упаковки

Именно тут обычно ломается “ощущение студийности”.

Самый важный практический вывод

Твои две допмодели я бы даже переименовал так:

не “модель угла съёмки”, а модель геометрии опорной сцены

не “модель освещения”, а модель studio lighting decomposition

И да — обе нужны.

Причём если выбирать, что важнее первым:

geometry model — обязательна для формы и привязки тени к полу

lighting model — обязательна для мягкости, длины, плотности и рефлексов

material/style model — следующий по важности модуль

Без geometry будет “плавающая” тень.
Без lighting будет “технически верная, но некрасивая” тень.
Без material head будет мало студийной правдоподобности на глянце и прозрачных объектах.

Что бы я выбрал тебе как опорные работы

Если сузить до самого полезного набора для проектирования:

Joint Shadow Generation and Relighting via Light-Geometry Interaction Maps (2026) — как идеологический ориентир: тень и relighting надо связывать через геометрию.
papers.cool

MetaShadow (CVPR 2025) — как ориентир на object-centered shadow reasoning.
Adobe Research
+1

Photorealistic Object Insertion with Diffusion-Guided Inverse Rendering (ECCV 2024) — как ориентир по восстановлению света/тона/материалов, но не как прямой прод-стек.
NVIDIA
+1

DiffusionLight / Turbo (CVPR 2024 / 2025) — как база для light-annotation teacher.
diffusionlight.github.io
+1

Depth Anything V2 — как практичный depth prior, если нужна скорость и generalization.
arXiv.gg

Если хочешь, следующим сообщением я могу собрать тебе уже не обзор, а конкретную проектную схему ShadowGen v2/v3: какие именно головы, какие лоссы, какой формат таргетов и как это уложить в твои 24 GB train / 11 GB inference.

## Turn 21 - User

давай прейдем к практическим схемам с выбором конкретных датасетов, архитектур, пайплайнов.
1. синтетический 3д датасет.
откуда берём модели?
чем рендерим? (у меня есть анриал энджайн (почти не умею пользоваться), блендер (немного умею), гудини (средне умею), c4d (хорошо умею) и гпу-рендер Redshift (высокое качество тени))
как автоматизировать создание сцен и рендер? как записывать данные сцен в датасет? может имеет смыслчасть сцен сделать реалтайм-рендеров, а часть редшифтом (или cicles)
рендерить несколько дней не проблема если всё будет автоматизированно, но неделями уже не хочется.

2. качественный датасет скорее всего буду генерировать через nana banana pro или seedream 4-5. артефактов я там особо не вижу. если бесплатно вручную то смогу несколько сотен сгенерировать, если автоматически по скрипту, то тоже наверное буду бюджетом ограничен, может до 1000 картинок.

3. пресказатель геометрии сцены и камеры.
какую конкретно архитектуру берем, какой пайплайн, как готовим данные
какие берем датасеты? наш синтетический и может что-то ещё есть опенсорсное на такую задачу?

4. предсказатель параметров освещения для разметки качественного датасета
какую конкретно архитектуру берем, какой пайплайн, как готовим данные
какие берем датасеты? наш синтетический и может что-то ещё есть опенсорсное на такую задачу? нужно ли делать 3 и 4 разными моделями или лучше разделить?

5. предобработка фото. в старом пайплайне у меня было так -
BLIP-2: Generates a textual description of the main object.
GroundingDINO: Zero-shot detection from the BLIP-2 prompt. (чтобы детектировать главный объект и отрезать остальное)
BiRefNet: Mate segmentation to extract the object mask.
в принципе работало, но если есть идеи на что заменить или как упростить, то предложи

6. генератор
тут нужно два решения. табильная рабочая лошадка (видимо на основе sd 1.5) и что-то более современное (проект я делаю для портфолио и хорошо бы что бы там были актуальные технологии)
желательно эти два решения сделать в едином пайплайне обучения

## Turn 22 - Assistant

Соберу это как инженерный план: отдельно проверю живые open-source датасеты/модели для 3D-объектов, освещения и геометрии, потом сведу к одному пайплайну, чтобы у тебя не получилось шесть несвязанных мини-проектов.

Да. Ниже дам не “обзор”, а рабочую схему проекта, с выбором того, что я бы реально делал на твоём месте.

Мой общий вывод такой:
ядро пайплайна лучше строить вокруг синтетики из 3D + маленького качественного real/gen набора + отдельных head-моделей для геометрии и света + двух генераторов в одном формате данных.
В качестве опоры на SOTA я бы брал не “одну чудо-модель”, а идеи из MetaShadow (объектно-центричный shadow reasoning), DIPIR (inverse rendering + генерация), DiffusionLight (аннотация света), Depth Anything V2 (быстрый depth prior), и свежую работу с LGI maps 2026, где тень и relighting связываются через геометрию.
OpenReview
+4
Adobe Research
+4
NVIDIA
+4

0. Что я бы выбрал как итоговую архитектуру проекта

Не один монолит, а 5 блоков:

Extract: вырезание объекта и маттинг.

Geometry: depth + plane/normal/contact.

Lighting: azimuth/elevation/softness/intensity/color/fill/bounce.

Coarse renderer: быстрый физический rough shadow/light layer.

Refiner / Generator: две версии на одних и тех же данных:

рабочая лошадка: SD1.5-class ControlNet/i2i;

современная ветка: SANA-0.6B / Sana-ControlNet style.

Такой модульный дизайн хорошо совпадает с тем, что сейчас считается сильным направлением: ControlNet-адаптеры на pretrained backbone, отдельные priors на геометрию/свет, и object-centered reasoning для тени.
OpenReview
+3
Hugging Face
+3
Hugging Face
+3

1. Синтетический 3D-датасет
Откуда брать модели

Я бы делал так:

Основной источник: ABO
Почему:

там 7,953 artist-created 3D-модели реальных household/product objects;

у них есть PBR-материалы;

это именно товарные и бытовые объекты, ближе к твоему домену, чем абстрактный интернет-мешанинный 3D.
Amazon Science
+1

Второй источник: ShapeNetCore
Почему:

около 51,300 моделей в 55 категориях;

хорошие категории для разнообразия формы.
Минус: материалы и “товарная” реалистичность слабее, чем у ABO.
shapenet.org
+1

Третий источник, осторожно: Objaverse / Objaverse-XL
Почему:

очень большой масштаб: Objaverse ~800K+, Objaverse-XL 10M+ объектов;

можно добрать сложные/редкие формы.
Но я бы фильтровал очень жёстко, потому что там много мусора, разношёрстных лицензий и кривых мешей.
Hugging Face
+1

Что брать practically

Для v1 я бы не распылялся:

ABO как база,

ShapeNetCore для добора форм,

Objaverse только точечно.

Чем рендерить

Я бы выбрал Blender + BlenderProc как основной генератор датасета.
Причина не в “самом красивом” рендере, а в том, что BlenderProc специально заточен под автоматическую генерацию реалистичных synthetic datasets на Python API и умеет сохранять стандартные CV-модальности вроде color/depth/normals/segmentation. Для автоматизации и выгрузки разметки он удобнее, чем Redshift/C4D как основной конвейер.
DLR
+1

Что делать с Redshift / C4D

Я бы не выбрасывал их. Я бы разделил так:

80–90% синтетики: Blender/BlenderProc + Cycles

10–20% premium subset: C4D + Redshift

Зачем premium subset:

для красивых soft shadows,

сложных цветных рефлексов,

прозрачностей,

глянца,

тонких penumbra.

То есть не весь датасет рендерить дорогим оффлайном, а только “teacher-quality subset” для hard cases и финетюна/refinement.

Имеет ли смысл мешать realtime и offline

Да, но в твоём случае я бы не делал Unreal основным.
Unreal удобен для realtime randomization, а для HDRI есть даже Poly Haven browser-плагин, но тебе сейчас важнее не UE-пайплайн, а быстрый и предсказуемый CV dataset generator. Поэтому Unreal я бы оставил как optional future branch. Для фонового studio lighting и автоматических аннотаций проще стартовать с BlenderProc. Poly Haven даёт CC0 HDRI и модели, что полезно и для Blender, и для UE.
Poly Haven
+2
Poly Haven
+2

Как автоматизировать сцены

Сцена на каждый sample:

1 объект

1 опорная плоскость

1 камера

1 key light

optional fill light

optional bounce card / weak colored floor bounce

optional HDRI окружение низкого вклада

Случайно варьируешь:

camera azimuth / elevation / focal length

object rotation / scale

plane tilt

light azimuth / elevation

light size

light intensity

light color temperature / tint

material overrides для части объектов

расстояние объекта до плоскости

белизну/оттенок фона

Что сохранять в датасет

На один sample я бы писал:

rgb.png
rgba_object.png
mask.png
depth.exr
normals.exr
shadow_rgba.exr
contact_shadow.png
light.json
camera.json
scene.json

В light.json:

azimuth_deg

elevation_deg

size

intensity

color_rgb

fill_ratio

bounce_strength

В camera.json:

focal_length / fov

camera pose

plane normal in camera coords

horizon line / tilt proxy

В scene.json:

object id

source dataset

material tags

renderer

seed

Сколько синтетики делать

Я бы целился в:

30k–50k для первого proof-of-concept,

80k–150k для уже нормального обучения генератора,

premium Redshift subset: 3k–8k.

Это не “официальное число из статьи”, а мой инженерный диапазон под твою задачу. Он хорошо согласуется с тем, что большие shadow/relighting работы выигрывают от синтетики и многопараметрического разнообразия, а OpenIllumination и другие inverse-rendering датасеты показывают ценность именно богатых разметок, а не просто количества JPG.
oppo-us-research.github.io
+2
Hugging Face
+2

2. Качественный датасет из Nana Banana / Seedream

Это имеет смысл, но я бы использовал его не как основной train set, а как:

style finetune set

high-quality refinement set

validation for realism

teacher set для рефлексов и “студийного вкуса”

Сколько нужно

Если вручную реально собрать 300–1000 качественных картинок, это уже полезно.
Но я бы делал не просто “картинки”, а старался получить пары:

object crop / mask

final studio image

по возможности разметку света через твой light estimator

по возможности pseudo depth / geometry

Этот набор лучше использовать:

для дообучения refiner,

для distillation визуального стиля,

для human-preference eval.

3. Предсказатель геометрии сцены и камеры
Делать отдельной моделью?

Да, отдельно.
Я бы не объединял geometry и lighting в одну голову на старте. У них разные признаки и разные типы ошибок. Отдельные модели проще дебажить.

Что именно предсказывать

Не “угол камеры вообще”, а:

relative depth

support plane mask

support plane normal

contact footprint

camera tilt / horizon

optional: coarse camera elevation bin

База

Depth Anything V2 как backbone/teacher — лучший практический выбор.
У него есть модели от 24.8M до 335.3M параметров, и авторы прямо пишут, что он быстрее и точнее SD-based depth моделей, а V2 обучен через синтетику + teacher-student схему, что как раз близко твоему сетапу.
GitHub
+1

Практический пайплайн

Я бы делал так:

Stage G1
Depth Anything V2-Small/Base → relative depth

Stage G2
Маленький U-Net / SegFormer-style head на входе:

RGB

mask

predicted depth

Выход:

plane mask

plane normal (3 channels, normalized)

contact map

horizon/tilt scalar

Данные

Для обучения geometry head:

твоя синтетика из BlenderProc — основа;

можно добавлять Hypersim / InteriorVerse как pretraining/support data для depth/plane priors, потому что там есть реалистичные indoor scenes, geometry и lighting annotations. InteriorVerse особенно ценен тем, что там более 50,000 изображений, 4000+ сцен и ground-truth geometry/material/lighting; для 1000 сцен есть HDR lighting.
HyperAI
+1

Но поскольку у тебя объект чаще уже вырезан и сцена минимальна, я бы не переоценивал чужие indoor datasets. Они полезны как pretraining prior, не как основной домен.

4. Предсказатель параметров освещения
Делать отдельной моделью?

Тоже да, отдельно.

Геометрия отвечает на “куда проецировать”.
Свет отвечает на “насколько длинно, мягко, плотно и какого цвета”.

Что предсказывать

Для твоего домена я бы не пытался восстанавливать полный HDR envmap. Лучше предсказывать компактный studio-light descriptor:

azimuth

elevation

size / softness

intensity

light color (RGB or temperature+tint)

fill ratio

bounce/reflex strength

optional second-light flag

На чём строить

Есть два хороших ориентира:

1. DiffusionLight — как teacher-идея для оценки освещения из одного изображения через “virtual chrome ball”. Это сильная опора для разметки качественного датасета.
GitHub

2. OpenIllumination — как реальный объектовый датасет с известным освещением, камерами и масками. Там 108K+ изображений 64 объектов, множество освещений и известные camera/light parameters. Это очень полезный внешний dataset именно для проверки и предварительной настройки light estimator.
oppo-us-research.github.io
+1

Конкретная архитектура

Я бы делал так:

L1. Teacher / offline annotator
DiffusionLight или похожая тяжёлая модель — для оффлайн pseudo-labeling части real/gen данных.
GitHub

L2. Production light regressor
Небольшой backbone, например:

DINOv2 small / ConvNeXt-T / ViT-S class

вход: RGB + mask + optional coarse depth

выход: несколько head’ов:

angle head

softness head

intensity head

color head

bounce head

Датасеты

Для обучения:

твой synthetic render set — основной,

OpenIllumination — для calibration/eval,

quality set — для pseudo-label finetune.
oppo-us-research.github.io
+1

5. Предобработка фото

Текущий пайплайн у тебя был:
BLIP-2 → GroundingDINO → BiRefNet

Он рабочий, но я бы упростил.

Что бы я делал сейчас

Вариант A, самый практичный

Grounded-SAM-2

если нужен alpha matte качества лучше — поверх BiRefNet или RMBG-2.0

Grounded-SAM-2 уже объединяет Grounding DINO / Florence-2 / SAM 2. SAM 2 официально имеет image/video predictor и training code, а Grounded-SAM-2 как раз делает связку grounding + segmentation.
GitHub
+1

Что заменить

BLIP-2 я бы убрал из обязательной цепочки.
Если у тебя домен “один главный объект на фото”, то дешевле и устойчивее:

либо пользовательский click/box,

либо open-vocab detector,

либо вообще background remover + largest foreground heuristic.

Что взять для маттинга

Если проект некоммерческий/исследовательский:

RMBG-2.0 интересен, потому что BRIA прямо позиционирует его как background-removal модель нового поколения, а их HF model card указывает, что архитектурная база — BiRefNet.
Hugging Face
+1

Если хочешь оставить всё максимально open и без лишних лицензионных сюрпризов:

SAM 2 / Grounded-SAM-2 для object selection,

BiRefNet-class matting для финальной альфы.

Мой выбор

Для v2:

Grounded-SAM-2 для поиска главного объекта,

BiRefNet/RMBG-2.0 для аккуратного matte,

убрать BLIP-2 из продового пути.

6. Генератор: два решения в одном пайплайне

Ты правильно хочешь две ветки:

A. Рабочая лошадка

SD 1.5 + ControlNet-style conditioning

Почему:

экосистема зрелая;

ControlNet официально рассчитан на дополнительное structural conditioning и обучается как адаптер поверх pretrained backbone; это как раз удобно под mask + depth + coarse shadow prior + light code.
Hugging Face
+1

Как именно
Не text-to-image в лоб, а:

base latent backbone SD1.5-class

control inputs:

mask

depth

plane/contact map

coarse shadow prior

target:

RGBA lighting residual

либо shadow_rgb + alpha

B. Современная ветка для портфолио

SANA-0.6B / SANA-ControlNet style

Почему:

это действительно современный backbone;

у SANA есть официальный ControlNet demo и fine-tuning/LoRA story;

0.6B официально заявлен как inference around 9GB VRAM, 1.6B — 12GB, а training docs для full training указывают 32GB VRAM. Это значит, что под твои 24GB я бы смотрел либо на 0.6B, либо на LoRA/adapter-only, а не на тяжёлый full finetune 1.6B.
nvlabs.github.io
+2
nvlabs.github.io
+2

Единый формат обучения для обеих веток

Сделай один dataset schema:

inputs

image

mask

depth

plane_normal

contact_map

coarse_shadow_prior

light_code

targets

lighting_rgba

final_composite

Тогда:

для SD1.5-ветки ты учишь ControlNet/i2i adapter;

для SANA-ветки ты учишь transformer-side adapter / ControlNet-like branch.

Что я бы реально выбрал первым

Сначала SD1.5-ветка — чтобы быстро получить рабочий baseline.

Потом тот же датасет и те же conditioning maps подключить к SANA-0.6B как research/portfolio branch.

Так у тебя не развалится проект на две независимые инфраструктуры.

Рекомендованный практический стек по пунктам
Синтетика

3D assets: ABO → ShapeNetCore → точечно Objaverse.
Hugging Face
+3
Amazon Science
+3
Amazon Web Services, Inc.
+3

Renderer: Blender + BlenderProc + Cycles как mainline.
DLR
+1

Premium subset: C4D + Redshift.

HDRI/light assets: Poly Haven CC0.
Poly Haven
+2
Poly Haven
+2

Geometry model

Depth backbone: Depth Anything V2 Small/Base.
GitHub
+1

Head: plane mask + normal + contact + tilt.

Lighting model

Teacher: DiffusionLight offline.
GitHub

Regressor: небольшой ViT/ConvNeXt head.

External real dataset: OpenIllumination.
oppo-us-research.github.io
+1

Preprocess

Grounded-SAM-2

BiRefNet/RMBG-2.0 for alpha matte.
GitHub
+3
GitHub
+3
GitHub
+3

Generator baseline

SD1.5 + ControlNet-style adapter.
Hugging Face
+1

Generator modern

SANA-0.6B + adapter / ControlNet-like conditioning.
nvlabs.github.io
+1

Что бы я делал по этапам
Этап 1

Собрать 30k–50k synthetic в BlenderProc:

ABO + ShapeNet

white/light backdrop

light code

RGBA lighting target

depth/normals/contact

Этап 2

Обучить:

geometry head

light head

SD1.5 baseline

Этап 3

Собрать 300–1000 quality images

Nana Banana / Seedream / real studio-like

разметить pseudo light + pseudo depth

сделать небольшой ручной eval set

Этап 4

Доучить:

refinement

SANA branch

hard cases: прозрачное, глянец, тонкие детали

Самое важное решение

Если совсем коротко:
для synthetic pipeline я бы выбрал BlenderProc, для 3D-источников — ABO как базу, для geometry — Depth Anything V2, для light labeling — DiffusionLight + OpenIllumination, для baseline generator — SD1.5 ControlNet, для “современной” ветки — SANA-0.6B adapter.

Это выглядит и реалистично под 24GB train / 11GB infer, и достаточно современно для портфолио.
nvlabs.github.io
+6
DLR
+6
Amazon Science
+6

Следующим сообщением я могу уже сделать совсем прикладную вещь:
дать тебе точную структуру папок датасета, JSON-схему sample’а и список полей/рендер-пассов, которые надо сохранять из BlenderProc/Redshift.

## Turn 23 - User

сделай самодостаточное ТЗ на первый пункт про синтетический датасет в md формате

## Turn 24 - Assistant

Markdown
# ТЗ: синтетический 3D-датасет для генерации студийных теней и рефлексов

## 1. Назначение

Разработать автоматизированный пайплайн генерации синтетического датасета для обучения моделей проекта ShadowGEN.

Датасет должен использоваться для задач:

1. генерации реалистичной тени объекта на светлом/белом фоне;
2. генерации цветных рефлексов и локального light interaction с опорной поверхностью;
3. обучения вспомогательных моделей:
   - предсказателя геометрии сцены и камеры;
   - предсказателя параметров освещения;
4. обучения основной генеративной модели:
   - baseline-ветка на SD 1.5-class backbone;
   - modern-ветка на более современной архитектуре.

---

## 2. Цели датасета

Сгенерировать синтетический датасет, в котором для каждого объекта и рендера доступны:

- финальное RGB-изображение;
- маска объекта;
- альфа объекта;
- карта глубины;
- карта нормалей;
- карта тени;
- карта контактной тени;
- карта lighting residual / shadow+reflex layer;
- параметры камеры;
- параметры освещения;
- параметры сцены;
- метаданные об объекте и материале.

Датасет должен покрывать:

- разные формы объектов;
- разные ракурсы камеры;
- разные направления света;
- разную мягкость тени;
- разные цветовые температуры и цветные рефлексы;
- разные типы материалов;
- разные типы контакта объекта с плоскостью.

---

## 3. Область применения

Датасет предназначен для генерации изображений вида:

**вход**:
- фото объекта;
- маска объекта;
- дополнительные priors (depth, geometry, light params);

**выход**:
- реалистичная студийная тень;
- цветные рефлексы;
- итоговый композит на белом или светлом фоне.

---

## 4. Требования к качеству

Датасет должен позволять обучать модель, которая:

- работает с объектами, снятыми под углом, а не только сверху;
- генерирует правдоподобную тень с учетом формы объекта;
- поддерживает управление направлением освещения;
- поддерживает управление мягкостью тени;
- поддерживает цветной bounce / color bleed;
- визуально стремится к уровню студийной предметной съемки.

---

## 5. Источники 3D-объектов

### 5.1 Основной источник
Использовать **ABO (Amazon Berkeley Objects)** как основной источник 3D-моделей.

Причины:
- высокая доля предметных и товарных объектов;
- наличие PBR-материалов;
- хорошее соответствие домену предметной съемки.

### 5.2 Дополнительные источники
Использовать при необходимости:

- **ShapeNetCore** — для расширения разнообразия форм;
- **Objaverse / Objaverse-XL** — точечно, после фильтрации;
- локальные модели и собственные ассеты — по мере необходимости.

### 5.3 Требования к фильтрации моделей
Перед использованием модели должны пройти фильтрацию.

Не использовать:
- сильно поврежденные меши;
- модели с невалидной топологией, сломанными UV или инвертированными нормалями;
- слишком низкополигональные примитивы без пользы;
- сцены вместо отдельных объектов;
- персонажей, если они не нужны для домена;
- очевидный мусор из больших scraped-коллекций.

Использовать предпочтительно:
- предметы с читаемой формой;
- объекты, имеющие контакт с плоскостью;
- объекты с выраженной геометрией, от которой заметно зависит тень;
- объекты разных классов: упаковка, посуда, обувь, техника, игрушки, флаконы, коробки, бытовые предметы.

---

## 6. Рендер-стек

### 6.1 Основной рендерер
Основной генератор датасета: **Blender + BlenderProc + Cycles**.

### 6.2 Причина выбора
BlenderProc использовать как основную платформу, потому что он подходит для:

- пакетной генерации CV-датасетов;
- автоматического управления сценами через Python;
- сохранения depth, normals, segmentation и прочих рендер-пассов;
- запуска большого числа сцен без ручной сборки.

### 6.3 Премиум-подмножество
Для части датасета использовать **Cinema 4D + Redshift** либо **Blender + Cycles с более дорогими настройками**.

Назначение premium subset:
- сложные мягкие тени;
- качественные цветные рефлексы;
- прозрачности;
- полупрозрачные материалы;
- глянец;
- сложные penumbra и bounce-light эффекты.

### 6.4 Соотношение
Рекомендуемое соотношение:

- **80–90%** данных — BlenderProc/Cycles;
- **10–20%** данных — premium subset (Redshift/Cycles HQ).

---

## 7. Общая структура сцены

Каждый sample должен содержать минимум:

- 1 объект;
- 1 опорную плоскость;
- 1 камеру;
- 1 ключевой источник света;
- optional fill light;
- optional bounce card / colored bounce;
- optional HDRI окружение с небольшим вкладом.

### 7.1 Фон
Основной фон:
- белый;
- светло-серый;
- светло-бежевый в небольшом диапазоне.

Фон должен быть нейтральным, без визуального шума.

### 7.2 Плоскость
Опорная поверхность:
- плоская;
- чаще всего горизонтальная;
- иногда с небольшим наклоном;
- матовая в большинстве сцен;
- в части сцен — полуматовая/глянцевая.

### 7.3 Положение объекта
Варианты:
- объект стоит на плоскости;
- объект слегка приподнят;
- объект имеет сложную опору;
- объект имеет ножки, ручки, полости, тонкие выступы.

---

## 8. Диапазоны рандомизации

### 8.1 Камера
Для каждой сцены случайно варьировать:

- азимут камеры;
- elevation камеры;
- дистанцию камеры;
- focal length / FOV;
- небольшие перспективные искажения;
- высоту горизонта;
- наклон плоскости в системе камеры.

### 8.2 Объект
Для объекта варьировать:

- rotation по вертикали;
- умеренный наклон;
- scale в разумных пределах;
- положение на плоскости;
- расстояние до плоскости;
- material override в части сцен;
- roughness override в части сцен;
- optional color override для части подмножества.

### 8.3 Свет
Для каждого sample варьировать:

- azimuth света;
- elevation света;
- intensity;
- size источника;
- distance источника;
- цвет света;
- fill ratio;
- bounce strength;
- наличие второго слабого источника.

### 8.4 Студийные сценарии света
Нужно подготовить несколько шаблонов света:

1. **hard side light**
2. **soft side light**
3. **top-side studio softbox**
4. **rear-side light**
5. **warm key + cool fill**
6. **cool key + warm bounce**
7. **large softbox with weak contact shadow**
8. **hard key + visible colored reflex**

---

## 9. Что именно нужно рендерить и сохранять

Для каждого sample нужно сохранить следующие артефакты.

### 9.1 Обязательные изображения
- `rgb.png` — финальный рендер сцены;
- `rgba_object.png` — объект с альфой;
- `mask.png` — бинарная маска объекта;
- `depth.exr` — глубина;
- `normals.exr` — нормали;
- `shadow_rgba.exr` — слой тени и светового взаимодействия;
- `contact_shadow.png` — карта контактной тени;
- `shadow_alpha.png` — плотность/интенсивность тени;
- `lighting_rgb.exr` — цветной residual / reflex layer.

### 9.2 Дополнительные карты
По возможности сохранять:
- `albedo.exr`;
- `roughness.exr`;
- `metalness.exr`;
- `object_id.png`;
- `plane_mask.png`;
- `plane_normal.exr`;
- `world_position.exr`;
- `ambient_occlusion.png`.

### 9.3 Рендер слоев
По возможности финальный sample должен быть раскладываем на:

- объект без тени;
- тень без объекта;
- color bleed / reflex без объекта;
- финальный композит.

---

## 10. Метаданные sample

Для каждого sample обязательно сохранять JSON-файл.

### 10.1 Структура файла `sample.json`
Обязательные поля:

```json
{
  "sample_id": "string",
  "object_id": "string",
  "source_dataset": "ABO | ShapeNet | Objaverse | custom",
  "renderer": "blender_cycles | redshift",
  "seed": 12345,
  "camera": {
    "azimuth_deg": 0.0,
    "elevation_deg": 0.0,
    "distance": 0.0,
    "fov_deg": 0.0,
    "tilt_deg": 0.0,
    "position": [0.0, 0.0, 0.0],
    "target": [0.0, 0.0, 0.0]
  },
  "plane": {
    "normal": [0.0, 1.0, 0.0],
    "tilt_deg": 0.0,
    "material_type": "matte | semi_gloss | gloss",
    "base_color": [1.0, 1.0, 1.0]
  },
  "light": {
    "azimuth_deg": 0.0,
    "elevation_deg": 0.0,
    "distance": 0.0,
    "size": 0.0,
    "intensity": 0.0,
    "color_rgb": [1.0, 1.0, 1.0],
    "color_temperature_k": 6500,
    "fill_ratio": 0.0,
    "bounce_strength": 0.0,
    "secondary_light": false
  },
  "object": {
    "rotation_deg": [0.0, 0.0, 0.0],
    "scale": 1.0,
    "offset_from_plane": 0.0,
    "material_class": "matte | glossy | transparent | mixed",
    "bbox_3d": [0.0, 0.0, 0.0]
  },
  "paths": {
    "rgb": "rgb.png",
    "rgba_object": "rgba_object.png",
    "mask": "mask.png",
    "depth": "depth.exr",
    "normals": "normals.exr",
    "shadow_rgba": "shadow_rgba.exr",
    "lighting_rgb": "lighting_rgb.exr",
    "contact_shadow": "contact_shadow.png"
  }
}
11. Требования к структуре каталога

Рекомендуемая структура:

dataset/
  train/
    0000001/
      rgb.png
      rgba_object.png
      mask.png
      depth.exr
      normals.exr
      shadow_rgba.exr
      shadow_alpha.png
      lighting_rgb.exr
      contact_shadow.png
      sample.json
    0000002/
      ...
  val/
    ...
  test/
    ...
  metadata/
    objects.csv
    render_log.csv
    split_manifest.json
12. Разделение на subset’ы

Датасет должен быть разбит минимум на следующие подмножества:

12.1 Core subset

Основной массовый набор:

BlenderProc + Cycles;

стандартные материалы;

большой разброс геометрии и света.

12.2 Premium subset

Небольшой, но качественный subset:

Redshift или дорогой Cycles;

более сложное освещение;

качественные рефлексы;

глянец, полупрозрачность, сложные материалы.

12.3 Hard cases subset

Отдельный набор сложных случаев:

прозрачные бутылки;

полупрозрачные упаковки;

объекты с тонкими ножками;

объекты с дырками и полостями;

объекты с ручками;

глянцевые и зеркальные поверхности;

сложные irregular silhouettes.

12.4 Calibration subset

Небольшой стабильный набор с фиксированными объектами и систематическим перебором параметров света и камеры.
Используется для:

отладки;

sanity-check;

оценки предсказателей геометрии и света.

13. Планируемый объем данных
13.1 MVP

Минимально жизнеспособный набор:

30 000 – 50 000 sample’ов.

13.2 Рабочий набор

Рекомендуемый размер:

80 000 – 150 000 sample’ов.

13.3 Premium subset

3 000 – 8 000 sample’ов.

13.4 Hard cases subset

2 000 – 5 000 sample’ов.

14. Баланс категорий

Необходимо следить, чтобы датасет не был перекошен только в сторону коробок и простых цилиндров.

Желательные группы объектов:

коробки и упаковка;

бутылки и флаконы;

чашки и кружки;

обувь;

бытовая техника;

пластиковые контейнеры;

игрушки;

косметические предметы;

предметы со сложной формой;

предметы с отверстиями и ручками;

полупрозрачные и глянцевые объекты.

15. Типы материалов

Нужно покрыть минимум следующие классы:

matte;

semi-matte;

glossy;

semi-transparent;

transparent;

plastic;

painted metal;

ceramic;

rubber-like;

mixed.

Для части сцен нужно делать controlled material override, чтобы одна и та же геометрия рендерилась с разными отражательными свойствами.

16. Требования к автоматизации
16.1 Полная автоматизация

Генерация датасета должна выполняться пакетно без ручной сборки сцен.

16.2 Скриптовый запуск

Нужен Python-скрипт или набор скриптов, который:

выбирает объект;

проверяет его пригодность;

строит сцену;

случайно задает параметры камеры;

случайно задает параметры света;

рендерит нужные пассы;

сохраняет изображения;

пишет JSON-метаданные;

логирует ошибки;

умеет продолжать с места падения.

16.3 Требования к логированию

Для каждого прогона нужно сохранять:

старт/финиш;

renderer;

object_id;

scene seed;

ошибки импорта;

ошибки рендера;

время на сцену;

число успешно сгенерированных sample’ов.

17. Требования к скорости генерации
17.1 Ограничения

Рендер нескольких дней допустим. Рендер неделями — нежелателен.

17.2 Целевой режим

Пайплайн должен быть рассчитан так, чтобы:

MVP-датасет можно было собрать за несколько дней;

premium subset — отдельным фоновым прогоном;

при необходимости можно было легко уменьшать качество отдельных пассов ради скорости.

17.3 Практический компромисс

Для основного корпуса использовать:

умеренное разрешение;

умеренное число сэмплов;

denoising;

batching.

Для premium subset:

более дорогой рендер;

выше разрешение;

более сложные световые сценарии.

18. Разрешение рендера
18.1 Базовое разрешение

Для train-корпуса:

минимум 768 px по длинной стороне;

предпочтительно 1024 px по длинной стороне.

18.2 Premium subset

1024–1536 px по длинной стороне.

18.3 EXR-пассы

Depth, normals, lighting и другие технические карты сохранять в EXR, даже если preview-версии дополнительно пишутся в PNG.

19. Контроль качества
19.1 Автоматические проверки

Нужно автоматически отбрасывать sample’ы, если:

объект вышел из кадра;

объект слишком мал;

маска пустая или почти пустая;

depth/pass поврежден;

тень обрезана;

слишком сильный пересвет;

сцена разрушена;

объект висит в воздухе без планируемой причины;

материал не отрендерился.

19.2 Визуальный QC

Для каждого N-го sample нужно автоматически формировать preview-контакт-лист:

RGB;

mask;

depth;

normals;

shadow layer;

metadata summary.

19.3 Ручная проверка

Нужно вручную просматривать случайную выборку из каждой партии рендера.

20. Требования к split

Нужно исключить утечку по геометрии между train/val/test.

20.1 Основное правило

Один и тот же объект object_id не должен одновременно попадать в train и val/test.

20.2 Рекомендуемый split

train: 80%

val: 10%

test: 10%

20.3 Hard generalization split

Дополнительно желательно сделать subset, где в test попадают unseen categories или unseen material setups.

21. Минимальные deliverables

По результату первого этапа должны быть готовы:

список источников 3D-моделей;

фильтр пригодных объектов;

автоматический генератор сцен;

пакетный рендер-пайплайн;

схема метаданных;

структура датасета;

MVP-датасет минимум на 30k sample’ов;

premium subset;

hard cases subset;

визуальный QC-report.

22. Критерии приемки

Первый пункт считается выполненным, если:

сгенерирован датасет не менее чем из 30 000 валидных sample’ов;

для каждого sample сохранены все обязательные изображения и JSON;

датасет включает не менее:

300 уникальных объектов;

5 классов материалов;

8 сценариев света;

3 уровня мягкости тени;

3 уровня цветных рефлексов;

train/val/test разделены без утечки объектов;

автоматический генератор может быть перезапущен после сбоя;

создан отдельный premium subset;

создан hard cases subset;

подготовлены preview-листы для QC;

датасет пригоден для обучения:

geometry predictor;

light predictor;

baseline generator.

23. Рекомендуемый порядок реализации
Этап 1

собрать 3D-источники;

написать фильтрацию;

запустить тестовые сцены.

Этап 2

реализовать BlenderProc pipeline;

сохранить все обязательные пассы;

проверить JSON-схему.

Этап 3

отладить randomization;

сделать calibration subset;

проверить покрытия диапазонов.

Этап 4

запустить массовый рендер core subset;

настроить QC.

Этап 5

запустить premium subset в Redshift/Cycles HQ;

отдельно собрать hard cases subset.

24. Основной результат этапа

Результатом работы должен стать полностью воспроизводимый синтетический датасет, пригодный как для обучения генератора студийных теней и рефлексов, так и для обучения вспомогательных моделей геометрии и освещения.

Если хочешь, следующим сообщением я сделаю вторую часть в таком же формате: **ТЗ на предсказатель геометрии сцены и камеры**.

## Turn 25 - User

можешь коротко по шагам как довести ABO до датасета.  я скачиваю уже модели, но как автоматизировать создание сцен и рендер? я бы хотел сделать разные углы камеры, от почти фронтальных до вида сверху, разыне азимуты. разные мыгкости тени (размер софтбокса) и наверно нужны ещё дополнительный заполнящий источник света что бы не совсем провал был в тенях, что бы как будто в белой студии снято с рампой (то есть без линии горизонта). размер 512x512. exr без особой нужды я бы не использовал

## Turn 26 - Assistant

Сейчас сведу это в короткий, рабочий pipeline без лишней теории: от скачанных ABO-моделей до пакетного рендера сцен с управляемой камерой и светом.

Да. Самый короткий рабочий путь такой:

Берёшь ABO как пул объектов и сначала не пытаешься использовать всё подряд. ABO хорош тем, что там есть тысячи artist-created 3D-моделей реальных household/product objects с physically based materials, то есть база для предметки уже подходящая.
Amazon Science
+1

Делаешь предфильтр моделей: импорт в Blender, авто-нормализация масштаба, центрирование по bbox, проверка на пустую геометрию, кривые нормали, слишком мелкий/слишком вытянутый объект. На этом этапе оставляешь только “нормально рендерящиеся” модели и записываешь их в свой objects_manifest.json. Это уже сильно экономит время на массовом прогоне. BlenderProc как раз удобен тем, что это Python-управляемый генератор synthetic CV datasets поверх Blender.
dlr.de

Делаешь одну базовую сцену-шаблон, а не собираешь сцены руками:

бесшовная белая “рампа” вместо пола+стены,

1 объект,

1 камера,

1 key area light,

1 fill area light слабее,

optionally very weak top/ambient light.
Для твоей задачи линия горизонта не нужна: объект должен стоять на изогнутой белой циклораме.

Камеру параметризуешь через углы, а не через ручное таскание:

azimuth: 0–360°

elevation: например 10–80°

radius: дистанция

fov: например 25–50°
Тогда ты легко получаешь и почти фронтальные виды, и сверху-сбоку, и почти сверху. Практически это один look_at(object_center) плюс перевод сферических координат в позицию камеры.

Свет тоже задаёшь параметрами, а не “на глаз”:

key_azimuth

key_elevation

key_power

softbox_size

fill_power_ratio

fill_color_temp
Мягкость тени контролируешь в первую очередь размером area light / softbox. Для белой студии логично держать fill всегда включённым, но слабым, например 0.15–0.35 от key, чтобы не было полного провала в тенях.

Автоматизация сцены делается циклом по объектам и random seeds. На каждый sample:

загружаешь объект,

ставишь на рампу,

рандомишь камеру,

рандомишь key/fill,

рендеришь,

сохраняешь PNG и JSON.
То есть по сути у тебя будет один Python-скрипт, который генерирует тысячи сцен из одного шаблона.

Что сохранять, если без EXR:

rgb.png

mask.png

shadow.png или shadow_rgba.png

depth.png в 16-bit, если depth нужен

normal.png только если реально будешь использовать

sample.json с параметрами сцены
Для старта этого хватит. Если хочешь упростить ещё сильнее, сначала сохраняй только rgb + mask + sample.json + optional shadow pass.

Что писать в sample.json:

object_id

camera_azimuth

camera_elevation

camera_fov

light_azimuth

light_elevation

light_size

light_intensity

fill_ratio

background_color

renderer

seed
Это потом даст тебе бесплатные GT-лейблы для моделей геометрии и освещения.

По рендереру я бы для массового корпуса выбрал Blender + Cycles / BlenderProc, а не Redshift/C4D. BlenderProc прямо заточен под генерацию реалистичных synthetic datasets и типовых CV-модальностей. Redshift имеет смысл оставить для маленького premium-поднабора, а не для всего корпуса.
dlr.de

По качеству/скорости для 512×512 я бы делал так:

основная масса: Cycles, умеренные samples, denoise on

premium subset: больше samples, сложнее материалы, maybe Redshift
Так ты сможешь за несколько дней получить нормальный корпус, а не рендерить неделями.

Первый MVP-план:

отфильтровать 300–500 ABO-объектов,

на каждый объект сделать 20–50 рендеров,

получить 10k–20k sample’ов,

проверить визуально,

потом уже наращивать покрытие по hard cases и материалам.
У ABO как раз есть несколько тысяч 3D-моделей и они ориентированы на реальные предметы, так что такой старт вполне реалистичен.
Amazon Science
+1

Совсем практично — минимальная логика генерации одного sample:

выбрать object_id

импортировать mesh

нормализовать размер

поставить origin на основание

поместить на циклораму

выбрать cam_azimuth/elevation/fov

выбрать light_azimuth/elevation/size/intensity

выставить fill

рендер

записать PNG + JSON

Если хочешь, следующим сообщением я могу дать тебе уже псевдокод Python/Blender для такого batch-рендера без лишней воды.

## Turn 27 - User

как это всё автоматизировать? не всевдокодом а в реале

## Turn 28 - Assistant

Я проверю актуальные форматы ABO и текущий способ автоматизации через Blender/BlenderProc, чтобы дать тебе не схему, а реально запускаемый каркас.

Сделаю тебе готовый каркас скрипта и рядом короткий README с командой запуска, чтобы это можно было сразу подправить под свою папку с ABO и пробовать.

Да. Я сделал тебе готовый стартовый каркас:

render_abo_dataset.py

README_render_abo_dataset.md

Что он уже делает:

берёт папку с ABO-моделями,

автоматически находит поддерживаемые 3D-файлы,

строит простую белую циклораму,

ставит объект,

рандомит камеру от почти фронтального до почти сверху,

рандомит key light и fill light,

меняет мягкость тени через размер area light,

рендерит rgb.png, mask.png, sample.json,

опционально пишет depth.png и normals.png.

Почему такой старт нормальный:

ABO реально содержит artist-created 3D-модели household/product objects с physically based materials.
Amazon Science
+1

BlenderProc специально заточен под procedural photorealistic rendering и умеет loading, cameras, lighting, RGB/depth/normal/segmentation рендеры.
dlr-rm.github.io
+1

Для запуска у BlenderProc есть стандартный режим blenderproc run ..., а quickstart официально именно так и проверяется.
GitHub
+1

Запуск для теста:

Bash
blenderproc run render_abo_dataset.py \
  --input /path/to/abo_models \
  --output /path/to/output_dataset \
  --views-per-object 4 \
  --limit 10 \
  --resolution 512

Что я бы правил первым под тебя:

форму циклорамы,

диапазоны cam_el, cam_fov,

диапазоны key_size и fill_ratio,

импорт конкретного формата ABO, если у тебя там окажется один доминирующий тип файла,

отдельный shadow pass, если захочешь именно слой тени, а не только финальный RGB.

Важная честная оговорка: это рабочий стартовый каркас, а не гарантированно идеальный production-скрипт под любую структуру ABO из коробки. У ABO/твоей локальной раскладки может понадобиться подправить импорт и нормализацию конкретных моделей. Но как база для реальной автоматизации он уже лучше, чем псевдокод.

Следующим шагом логично сделать версию 2:

отдельный shadow.png,

train/val split,

фильтр плохих объектов,

и manifest по классам/материалам.

## Turn 29 - User

tar в винде обязательно весь распаковывать для работы или можно как-то сразу с аривом работать?

## Turn 30 - Assistant

Обычно для Blender/BlenderProc и массового рендера tar лучше распаковать.

Напрямую с .tar работать можно только если ты сам напишешь слой, который:

читает архив,

временно извлекает нужный файл,

потом импортирует его в Blender.

Но на практике это неудобно, потому что:

Blender импортирует обычные файлы по пути, а не содержимое tar;

случайный доступ к тысячам файлов внутри архива медленный;

отладка и фильтрация моделей становятся сильно сложнее.

Самый нормальный вариант на Windows:

распаковать архив один раз;

получить обычную папку с моделями;

уже по ней гонять скрипт.

Если места мало, есть компромисс:

распаковать не всё подряд, а только папки с 3D-моделями;

текстуры/метаданные тоже рядом, если они нужны;

остальное можно не трогать.

Если ABO у тебя лежит в виде множества .tar, то лучше сделать отдельный скрипт:

проходит по архивам,

вытаскивает только нужные расширения (.obj, .glb, .gltf, .fbx, текстуры),

складывает в нормальную структуру папок.

То есть ответ: не обязательно распаковывать вообще всё содержимое датасета, но для реальной работы рендер-пайплайна нужные модели лучше распаковать на диск.
С .tar “на лету” я бы не работал.

Могу сразу дать тебе маленький Python-скрипт для Windows, который вытаскивает из .tar только 3D-модели и текстуры в отдельную папку.

## Turn 31 - User

как лучше фильтровать датасет? мне всякиая мебель и крупные вещи не оч нужны. в основном нужны предметы мелких и средних размеров

## Turn 32 - Assistant

Сейчас быстро посмотрю, какие у ABO есть удобные поля для отсева крупных вещей, чтобы предложить фильтр не “на глаз”, а по метаданным плюс геометрии.

Да. Лучший фильтр я бы делал в 3 слоя: по метаданным, по геометрии меша и по быстрому превью-рендеру.

Почему так: в ABO есть не только 3D-модели, но и metadata вроде product type, material, dimensions и других атрибутов, а сами 3D-модели для 7,953 товаров даны как glTF 2.0 с понятными соглашениями по ориентации и опоре на плоскость. Это позволяет довольно хорошо отсеивать крупняк ещё до рендера.
Amazon Science
+1

Как бы я фильтровал practically
1. Сначала по category / product_type

Сразу выкидываешь всё, что похоже на:

sofa, couch

chair, bench, stool

table, desk

bed

cabinet, dresser, shelf, bookcase

lamp floor

mirror large

rug, carpet

appliance large

outdoor furniture

А оставляешь то, что ближе к предметке:

mug, cup, bowl, plate

bottle, jar, container

toy, figurine

shoe, sneaker, sandal

electronics small

beauty/cosmetics

kitchenware small

storage box small

decor small/medium

В ABO как раз есть product listings с product type и dimensions, так что этот слой самый дешёвый.
Amazon Science
+1

2. Потом по physical dimensions

Даже внутри “нормальных” категорий бывают огромные объекты.
Поэтому второй фильтр — по размерам из metadata.

Я бы оставлял примерно так:

max dimension <= 60–70 cm для основного корпуса

отдельно subset “medium” до 90 cm

всё, что больше — в мусор или в отдельную папку

Для твоей задачи предметки это очень разумно: тебе важнее мелкие и средние объекты, а не диваны и тумбы.

3. Потом по bbox самого меша

Метаданные могут врать или быть неполными, поэтому после импорта в Blender:

считаешь 3D bbox,

проверяешь aspect ratio,

проверяешь объём bbox,

отбрасываешь экстремально плоские/длинные штуки.

Я бы выкидывал:

слишком плоские (min_dim / max_dim < 0.03)

слишком длинные и узкие (max_dim / median_dim > 12)

подозрительно огромные bbox после нормализации

пустые меши / мусор из нескольких случайных частей

4. Потом быстрый preview render

Очень полезный этап.
На каждый объект делаешь 1–2 быстрых рендера:

фронтально

сверху-сбоку

И автоматически выбрасываешь, если:

объект почти пустой в кадре

silhouette слишком маленький

объект визуально “ломаный”

текстуры не подтянулись

модель стоит криво

выглядит как мебель/часть интерьера, хотя category не помогла

Это самый надёжный последний фильтр.

Какой practical pipeline я бы сделал
Папка 1: keep_small_medium

Туда идут объекты для основного датасета.

Критерии:

category whitelist

max dimension <= 70 cm

bbox ok

preview ok

Папка 2: keep_medium_hard

Туда:

объекты до 90 cm

сложная форма

интересные ручки, ножки, полости

полезны для hard cases

Папка 3: reject_large

Туда вся мебель и крупняк.

Папка 4: reject_bad_mesh

Туда:

битые модели

странная геометрия

мусорные ассеты

плохо стоящие объекты

Что бы я делал по категориям
Точно оставить

кружки, чашки

бутылки, банки, флаконы

косметика

игрушки

обувь

коробки и упаковка

мелкая электроника

кухонные мелочи

небольшие контейнеры

декор настольного размера

Оставлять выборочно

настольные лампы

небольшие корзины

маленькие табуреты/подставки

medium decor objects

Скорее выкинуть

диваны

кресла

столы

тумбы

шкафы

кровати

большие зеркала

ковры

крупная техника

Самый полезный признак для тебя

Я бы ориентировался не только на “размер”, а на вопрос:

“похож ли объект на то, что реально снимают как отдельный предмет на белой студии?”

Если ответ “да” — оставлять.
Если это уже “кусок интерьера” — выкидывать.

Если совсем коротко, мой рецепт

Whitelist категорий

Отсев по dimensions

Проверка bbox меша

1–2 preview render

Ручная чистка первых 500–1000 объектов

После первой ручной чистки у тебя быстро появится хороший whitelist/blacklist слов, и дальше фильтрация станет почти автоматической.

Если хочешь, я могу следующим сообщением дать тебе готовый whitelist/blacklist слов для ABO metadata и логику фильтра в виде маленького Python-скрипта.

## Turn 33 - User

Uploaded a file
надо сформулировать тз на разработку мл сервиса. предполагем что модели мы уже обучили и надо сделать уже продовый сервис. пока прсото питон скрипт. в перспективе докер и тритон

во вложении контрактные требования к сервису

как должен работать (как я вижу. не приказ, обсуждаемо если есть идеи как улучгшитьб)
получаем фото на вход
выявляем главный объект, кропим и ресайзим до нужного размера (по краям оставляем поля - место дял тени) пока будет  512x512
прогоняем через модель определения угла обзора камеры и угла наклона камеры (это видимо лучше делать до вырезания из фона)
делаем маску вырезания из фона
делаем карту глубины, карту нормалей (это видимо уже лучше без фона делать)
кэшируем этот результат предобработки
подаём всё в генератор тени,
отдаём результат

## Turn 34 - Assistant

Да. Ниже — компактное, но уже продовое ТЗ на Python ML service v1, согласованное с контрактом из файла: сервис должен быть stateless, synchronous per request, с endpoint’ами GET /health, GET /v1/capabilities, POST /v1/render, machine-readable errors, одной финальной артефактной картинкой и опциональными debug-артефактами. Это прямо соответствует требованиям контракта.

new-ml-service

ТЗ на разработку ML-сервиса ShadowGen v1
1. Цель

Разработать продовый ML-сервис, который по одному входному изображению:

находит главный объект,

оценивает геометрию съёмки,

вырезает объект,

строит вспомогательные карты,

генерирует тень по явным параметрам света,

композитит результат на заданный фон,

возвращает финальное изображение и опциональные debug-артефакты.

Сервис реализуется как синхронный stateless Python HTTP service.
В перспективе архитектура должна без существенной переработки переноситься в Docker и далее в Triton / model serving runtime.

2. Границы ответственности сервиса
Сервис делает

принимает один источник изображения на запрос;

валидирует запрос;

декодирует изображение;

запускает полный inference pipeline;

возвращает финальный результат;

возвращает debug-артефакты по запросу;

возвращает metrics и warnings.

Сервис не делает

очереди задач;

хранение job state;

загрузку в облачные бакеты;

user management;

backward compatibility со старым ShadowGEN transport;

batch inference в v1.

Это совпадает с контрактом: один синхронный запрос, одна финальная артефактная картинка, без оркестрации и без cloud-coupling.

new-ml-service

new-ml-service

3. API-контракт
Обязательные endpoint’ы

GET /health

GET /v1/capabilities

POST /v1/render

Это обязательное требование контракта.

new-ml-service

Формат входа

POST /v1/render принимает JSON с:

request_id

pipeline_version

source

shadow

background

output

Поддержка полей angle_deg, elevation_deg, softness, opacity, reflection, background.mode=solid, output.return_debug обязательна.

new-ml-service

new-ml-service

Формат выхода

Успешный ответ обязан содержать:

artifacts

metrics

warnings

model_info

Должен быть ровно один final artifact. Debug-артефакты — только если return_debug=true. metrics.total_ms обязателен всегда.

new-ml-service

Ошибки

Обязательная machine-readable форма:

JSON
{
  "error": {
    "code": "validation_error",
    "message": "..."
  }
}

Категории:

validation_error

unsupported_input

processing_failed

timeout

internal_error

new-ml-service

4. Рабочий пайплайн inference

Ниже — рекомендованный v1 pipeline с учётом твоего описания и контракта.

Шаг 1. Decode + validate

Вход:

source.mime_type

source.image_base64

Что делаем:

проверяем MIME;

декодируем base64;

читаем изображение;

проверяем размер, битность, цветовое пространство;

нормализуем в RGB uint8 / float32.

Выход:

input_rgb

decode_ms

Шаг 2. Detection / subject localization

Что делаем:

определяем главный объект на фото;

строим bbox основного объекта;

оцениваем confidence;

вычисляем crop с безопасными полями под будущую тень.

Требование:

crop должен включать запас по краям;

целевой внутренний рабочий размер v1: 512×512.

Правило:

если confidence низкий, но объект выделен, сервис продолжает работу и пишет warning;

если объект не найден — processing_failed или validation_error, в зависимости от причины.

Шаг 3. Geometry-from-full-image

Этот шаг выполняется до удаления фона, как ты и предлагаешь.

Что делаем:

предсказываем:

угол обзора / camera elevation proxy,

tilt / наклон камеры,

plane / support geometry proxy.

Почему до вырезания:

фон и перспектива помогают понять геометрию съёмки.

Выход:

camera_geom

geometry_ms

Шаг 4. Background removal / cutout

Что делаем:

строим точную маску объекта;

вырезаем объект из фона;

создаём cutout RGBA.

Выход:

mask

cutout_rgba

segmentation_ms

Шаг 5. Crop + resize + padding

Что делаем:

кропим основной объект;

добавляем поля для будущей тени;

приводим к 512×512.

Требование:

паддинги должны зависеть от предполагаемого направления тени;

но в v1 допустимо делать симметричный padding с коэффициентом запаса.

Выход:

crop_rgb_512

crop_mask_512

crop_rgba_512

Шаг 6. Depth + normal estimation

Этот шаг выполняется по cutout / foreground-centered input, как ты и предлагаешь.

Что делаем:

строим monocular depth;

строим normal map;

optional: contact map / support map.

Выход:

depth_512

normals_512

depth_ms

Шаг 7. Preprocess cache

Что делаем:

сохраняем промежуточные результаты в кэш по ключу, зависящему от:

хэша входного изображения,

версии моделей preprocessing,

размера пайплайна.

Кэшируем:

bbox / crop info

camera geometry

mask

cutout

depth

normals

Не кэшируем:

финальную тень, потому что она зависит от shadow.* и background.*

Шаг 8. Shadow generation

Вход:

crop_rgb_512

crop_mask_512

depth_512

normals_512

camera_geom

явные пользовательские параметры:

angle_deg

elevation_deg

softness

opacity

reflection

Что делаем:

генератор строит shadow/light interaction layer;

отдельно может строить:

shadow_alpha

shadow_rgb

reflection layer

Выход:

shadow_rgba

shadow_ms

Шаг 9. Composition

Что делаем:

создаём requested background;

композитим объект;

композитим тень;

применяем output resize, если запрошен.

Выход:

final_image

composition_ms

Шаг 10. Encode response

Что делаем:

кодируем final в png/webp;

при return_debug=true кодируем debug-артефакты;

собираем response body.

Выход:

encode_ms

total_ms

5. Состав debug-артефактов

Если output.return_debug=true, сервис должен по возможности возвращать:

cutout

mask

crop

depth

normals

shadow

final

Минимально полезный набор:

final

cutout

shadow

mask

Это хорошо ложится на контракт, где debug-артефакты допускаются как отдельные artifacts[].

new-ml-service

6. Рекомендуемая внутренняя архитектура Python-сервиса
HTTP layer

Рекомендуется:

FastAPI

Pydantic v2 для строгой валидации схем

Uvicorn для локального и серверного запуска

Internal modules

Структура:

api/

schemas/

pipeline/

models/

cache/

utils/

tests/

Pipeline decomposition

Рекомендуемые интерфейсы:

Detector

GeometryEstimator

Segmenter

DepthEstimator

NormalEstimator

ShadowGenerator

Composer

ArtifactEncoder

Model loading

Модели должны:

загружаться один раз на старте;

иметь явную версию;

быть thread-safe или process-safe;

не перезагружаться на каждый запрос.

7. Кэширование
Цель

Ускорить повторные вызовы для одного и того же изображения при разных параметрах света.

Что кэшируется

По ключу:
sha256(image_bytes) + preprocess_model_versions + target_size

Сохраняем:

detected_bbox

camera_geom

mask

cutout_rgba

crop_rgb_512

crop_mask_512

depth_512

normals_512

Формат кэша

На первом этапе допустимо:

filesystem cache

joblib / pickle для metadata

PNG/NPY/NPZ для карт

Ограничения

кэш должен быть optional;

отключение кэша не должно ломать сервис;

сервис остаётся stateless на уровне API, даже если локальный кэш существует.

8. Производительность
Целевые метрики v1

один запрос за раз;

рабочий размер: 512×512;

целевой total_ms — определяется моделью, но должен логироваться всегда;

metrics.total_ms обязателен по контракту.

new-ml-service

Стадии, для которых желательно возвращать метрики

decode_ms

detection_ms

geometry_ms

segmentation_ms

depth_ms

shadow_ms

composition_ms

encode_ms

9. Warnings

Сервис должен уметь завершаться успешно, но возвращать warnings.

Примеры:

main_object_low_confidence

geometry_estimation_low_confidence

foreground_mask_low_confidence

reflection_clamped

debug_artifact_unavailable

Warnings обязательны как поле ответа даже если массив пустой.

new-ml-service

10. Требования к детерминизму

Сервис должен быть “deterministic enough for repeated use with the same input and settings”, как указано в контракте.

new-ml-service

Практически это значит:

фиксированный inference mode;

отключённый train mode;

фиксированные seed там, где это применимо;

отсутствие скрытых fallback-веток.

11. Логирование

Структурированные логи должны содержать:

timestamp

request_id

service_version

model_version

total_ms

stage timings

error code on failure

Нельзя логировать:

полный base64 payload

пользовательское изображение целиком в лог

Это соответствует контракту.

new-ml-service

12. Acceptance criteria для v1

Сервис считается готовым, если:

Реализованы:

GET /health

GET /v1/capabilities

POST /v1/render

POST /v1/render:

принимает один input image;

принимает angle_deg и elevation_deg;

валидирует softness, opacity, reflection, background, output.

На успехе:

всегда возвращается ровно один final artifact;

всегда возвращается metrics.total_ms;

всегда возвращаются warnings;

при return_debug=true сервис возвращает debug-артефакты, если они доступны.

На ошибке:

возвращается machine-readable error.code и error.message.

Pipeline реально выполняет:

detection/crop

geometry estimation

segmentation

depth/normals

shadow generation

composition

Есть unit tests на валидацию запроса и integration tests минимум на:

JPEG

PNG

change of angle_deg

change of elevation_deg

return_debug=true

invalid MIME type

timeout behavior

Это полностью совпадает с чеклистом контракта.

new-ml-service

13. Что я бы улучшил в твоей схеме

Твоя логика в целом правильная. Я бы поменял только две вещи.

1. Detection и geometry — до маски

С этим я согласен.

2. Crop лучше делать в два этапа

Не сразу “вырезали и 512×512”, а:

сначала rough detect bbox,

затем geometry on full image,

затем fine crop + mask + padding,

потом уже 512×512.

Так меньше шанс потерять контекст, нужный для оценки камеры.

14. Самая практичная формулировка пайплайна в одну строку

Input image → detect main object → estimate camera/scene geometry on original image → segment foreground → crop/pad/resize to 512 → estimate depth/normals on cutout → cache preprocess → generate shadow from explicit light params → composite on requested background → return final + debug artifacts

Если хочешь, я следующим сообщением превращу это в готовый .md документ без объяснений, уже в формальном стиле “для команды разработки”.

## Turn 35 - User

ок, только под "угол обзора" я имел ввиду "camera fov" но это я уже сам поправлю.

теперь напиши рекомендации по готовым моделям с хагинсфейс. что брать как юзать

## Turn 36 - Assistant

Соберу это по блокам пайплайна: что брать с Hugging Face для детекта, маттинга, глубины, нормалей и генерации, с упором на то, что реально можно поднять в Python-сервисе, а не просто существует.

Да. Ниже — практические рекомендации по готовым моделям с Hugging Face под твой сервис: что брать, где использовать, и что я бы выбрал как v1 production stack.

Сразу главный вывод:

Для продового сервиса я бы брал

детект: IDEA-Research/grounding-dino-base

маска/маттинг: ZhengPeng7/BiRefNet_lite-matting или ZhengPeng7/BiRefNet-matting

depth: depth-anything/Depth-Anything-V2-Small-hf или ...Base-hf

normals: сначала вообще вычислять из depth, а не отдельной моделью; если нужна отдельная модель — prs-eth/marigold-normals-v1-1

генератор baseline: stable-diffusion-v1-5/stable-diffusion-v1-5 + свой ControlNet/adapter

генератор modern: Efficient-Large-Model/Sana_Sprint_0.6B_1024px_diffusers как research/portfolio ветка

Почему именно так:

это всё реально поднимается в Python,

стеки уже есть в HF transformers / diffusers,

и это хорошо ложится на твой пайплайн сервиса.
Hugging Face
+5
Hugging Face
+5
Hugging Face
+5

1. Детект главного объекта
Что брать

IDEA-Research/grounding-dino-base

Почему:

это open-set detector;

можно искать “main object” по короткому prompt;

есть готовая интеграция через transformers;

модель на HF живая и массово используется.
Hugging Face
+1

Когда брать tiny

IDEA-Research/grounding-dino-tiny
Если захочешь ускорить pipeline и согласен на чуть более слабый recall. Он тоже есть на HF.
Hugging Face

Как использовать

Для твоего кейса я бы не строил сложные подписи через BLIP-2.
Лучше так:

prompt: "object."

fallback prompt: "product. item. object."

выбираешь самый крупный confident bbox

затем расширяешь bbox на запас под тень

Это проще и для сервиса надёжнее, чем BLIP-2 → prompt → detector.

Чем грузить
Python
from transformers import AutoProcessor, AutoModelForZeroShotObjectDetection

Это прямо показано в model card Grounding DINO.
Hugging Face

Моя рекомендация

Прод v1: grounding-dino-base
Fast fallback: grounding-dino-tiny

2. Маска / вырезание объекта

Тут есть важный практический момент:
если сервис потенциально коммерческий, надо смотреть на лицензию.

Вариант 1 — safest open choice

ZhengPeng7/BiRefNet_lite-matting

Почему:

официальные веса BiRefNet на HF;

MIT license;

легкая версия;

подходит для продового background removal / matting.
Hugging Face

Вариант 2 — качественнее

ZhengPeng7/BiRefNet-matting
или
wu1g119/BiRefNet_HR-matting

Почему:

это уже более сильный matting-вариант;

HR-версия обучалась для high-resolution matting;

на HF есть официальные/дублированные веса, и в model card прямо указано, что это high-resolution dichotomous segmentation / matting.
Hugging Face
+2
Hugging Face
+2

Что я бы не брал как основу без проверки лицензии

briaai/RMBG-2.0

Почему:

модель хорошая;

но на HF явно указано CC BY-NC 4.0 и отдельно сказано, что коммерческое использование требует коммерческого соглашения.
Hugging Face

Как использовать

Я бы делал так:

Grounding DINO даёт bbox,

BiRefNet получает crop с запасом,

возвращает alpha/mask,

потом ещё небольшая постобработка:

убрать мелкий мусор,

largest connected component,

feather only if needed.

Моя рекомендация

Прод v1: ZhengPeng7/BiRefNet_lite-matting
Качественный режим: ZhengPeng7/BiRefNet-matting или wu1g119/BiRefNet_HR-matting

3. Depth
Что брать

depth-anything/Depth-Anything-V2-Small-hf для сервиса
depth-anything/Depth-Anything-V2-Base-hf для более качественного режима

Почему:

есть HF-совместимые checkpoints;

есть поддержка через transformers;

авторы прямо пишут, что V2 более детальный, более robust, и 10x faster и lighter, чем SD-based depth models.
Hugging Face
+2
Hugging Face
+2

Почему не Marigold depth как основной

Marigold классный, но он diffusion-based. Для продового сервиса на speed budget это хуже. Hugging Face docs прямо описывают Marigold как diffusion-based pipeline для dense CV.
Hugging Face

Что выбрать по размеру

Small — продовый дефолт

Base — если качество заметно лучше на твоих данных и latency ещё приемлема

Large — скорее offline / annotator, не основной сервис

Чем грузить
Python
from transformers import AutoImageProcessor, AutoModelForDepthEstimation

или готовые примеры из docs/model card.
Hugging Face
+1

Моя рекомендация

Прод v1: depth-anything/Depth-Anything-V2-Small-hf
HQ режим: depth-anything/Depth-Anything-V2-Base-hf

4. Normals

Здесь мой практический совет немного отличается от “взять отдельную модель”.

Что я бы сделал сначала

Не брать отдельную normal model в v1, а считать normals из depth:

depth → smoothed gradients → normals in camera space

Почему:

меньше зависимостей,

быстрее,

консистентно с твоим depth,

проще кэшировать.

Для shadow generation этого очень часто хватает.

Если всё же нужна отдельная модель

prs-eth/marigold-normals-v1-1

Почему:

Hugging Face docs прямо рекомендуют его для MarigoldNormalsPipeline;

pipeline уже готов в diffusers.
Hugging Face

Почему с осторожностью

Это diffusion-based normals estimator, а значит:

тяжелее,

может быть избыточен для сервиса,

лучше подходит как offline annotator или quality mode.

Альтернатива попроще

sashasax/omnidata_normal_dpt_hybrid_384

Почему:

старый, но простой и понятный DPT-подход;

модель именно для monocular surface normal estimation.
Hugging Face

Моя рекомендация

Прод v1: normals считать из depth
Offline/HQ: prs-eth/marigold-normals-v1-1
Fallback: sashasax/omnidata_normal_dpt_hybrid_384

5. Lighting / intrinsic priors

Ты раньше спрашивал про отдельный предсказатель света. На HF для этого сейчас лучше смотреть не на “готовый студийный регрессор”, а на Marigold intrinsics как teacher/offline source.

Что есть

Hugging Face docs по Marigold прямо перечисляют:

prs-eth/marigold-iid-appearance-v1-1

prs-eth/marigold-iid-lighting-v1-1

И описывают это как intrinsic image decomposition для appearance/materials/lighting.
Hugging Face

Как я бы это использовал

Не как продовую онлайновую модель, а как:

offline annotator,

помощь при разметке quality dataset,

teacher для обучения твоего маленького light regressor.

Моя рекомендация

Не тащить в online v1
Использовать offline для pseudo-labeling и анализа.

6. Генератор: baseline
Что брать

stable-diffusion-v1-5/stable-diffusion-v1-5

Почему:

это нормальный диффузионный базовый checkpoint в diffusers;

модель исторически обучалась на 512×512, что совпадает с твоим рабочим размером;

экосистема ControlNet/adapter под SD1.5 огромная.
Hugging Face
+1

Что добавить

Под твою задачу я бы не использовал голый SD1.5 pipeline.
Нужна схема:

base: stable-diffusion-v1-5/stable-diffusion-v1-5

conditioning:

mask

depth

optional normals/contact map

light code

сверху свой:

ControlNet-like branch

или T2I-Adapter style branch

или UNet finetune on concatenated conditions

Что можно взять как reference

Для depth-conditioned baseline есть готовый checkpoint:
lllyasviel/control_v11f1p_sd15_depth.
Model card прямо говорит, что это ControlNet v1.1 depth checkpoint для use with Stable Diffusion, в частности с SD1.5.
Hugging Face
+1

Как использовать practically

Для продового проекта:

сначала обучаешь собственный shadow-generator branch на SD1.5;

а control_v11f1p_sd15_depth используешь как reference по коду/формату pipeline, не как финальный shadow model.

Моя рекомендация

Baseline generator: stable-diffusion-v1-5/stable-diffusion-v1-5

7. Генератор: современная ветка
Что брать

Efficient-Large-Model/Sana_Sprint_0.6B_1024px_diffusers

Почему:

это живой SANA/SANA-Sprint checkpoint на HF;

model card описывает его как ultra-efficient diffusion model;

там отдельно отмечена ControlNet integration.
Hugging Face
+1

Почему именно 0.6B

Потому что это наиболее реалистичный размер для твоего стека по inference и experimentation.

Как использовать

Не как готовый сервисный генератор “из коробки”, а как:

research branch,

portfolio branch,

второй backend для того же формата conditioning maps.

Моя рекомендация

Modern branch: Efficient-Large-Model/Sana_Sprint_0.6B_1024px_diffusers

8. Что я бы реально собрал в сервисе v1
Production stack v1

detector: IDEA-Research/grounding-dino-base

matting: ZhengPeng7/BiRefNet_lite-matting

depth: depth-anything/Depth-Anything-V2-Small-hf

normals: из depth

generator: свой shadow model на базе stable-diffusion-v1-5/stable-diffusion-v1-5

Quality / offline tools

HQ matting: wu1g119/BiRefNet_HR-matting

HQ depth: depth-anything/Depth-Anything-V2-Base-hf

HQ normals: prs-eth/marigold-normals-v1-1

intrinsics/lighting annotator: Marigold IID checkpoints

Portfolio / modern branch

generator modern: Efficient-Large-Model/Sana_Sprint_0.6B_1024px_diffusers

9. Что убрать из старого пайплайна
BLIP-2

Я бы убрал из продового сервиса.
Причина простая: для “найти главный объект” это лишняя ступень, latency и источник нестабильности.

GroundingDINO оставить

Да.

BiRefNet оставить

Да.

То есть вместо:

BLIP-2 → GroundingDINO → BiRefNet

я бы делал:

GroundingDINO → BiRefNet

или вообще:

BiRefNet first-pass → largest foreground

GroundingDINO as fallback

10. На что обязательно смотреть кроме качества
Лицензии

Очень важно:

RMBG-2.0 — non-commercial unless separately licensed.
Hugging Face

у разных Depth Anything checkpoint’ов лицензии на HF отличаются: есть apache-2.0, есть cc-by-nc-4.0. Это надо проверять именно на том checkpoint, который возьмёшь.
Hugging Face
+2
Hugging Face
+2

stable-diffusion-v1-5 — CreativeML OpenRAIL-M.
Hugging Face

Runtime

transformers удобно для Grounding DINO и Depth Anything V2

diffusers удобно для SD1.5, SANA, Marigold

matting-модели типа BiRefNet часто идут с custom code, это надо учитывать в сервисной упаковке.
Hugging Face
+1

11. Самый короткий practical shortlist

Если совсем без лишнего:

Брать

IDEA-Research/grounding-dino-base

ZhengPeng7/BiRefNet_lite-matting

depth-anything/Depth-Anything-V2-Small-hf

stable-diffusion-v1-5/stable-diffusion-v1-5

Опционально

prs-eth/marigold-normals-v1-1

Efficient-Large-Model/Sana_Sprint_0.6B_1024px_diffusers

Не брать как default для прод сервиса

briaai/RMBG-2.0, если нужен коммерческий self-host без отдельной лицензии.
Hugging Face

Если хочешь, следующим сообщением я могу собрать тебе это уже в виде таблицы для ТЗ: компонент → модель HF → лицензия → runtime → роль в пайплайне → prod/offline/research.

## Turn 37 - User

ок но напиши более четкий вариант без сомнений.
sd 1.5 не пиши, напиши что пока вместо модели поставим заглушку, которая делает самую примитивную тень размытием.

и может есть готовые модели которые параметры камеры (tilt, fow) смогут определить?

## Turn 38 - Assistant

Проверю, что сейчас есть из готовых моделей именно под tilt/FOV и horizon/camera calibration, чтобы дать тебе уже жёсткий shortlist без “можно попробовать”.

Да. Вот жёсткий рекомендованный стек без вариантов “можно попробовать”.

Что ставим в сервис v1
1. Детект главного объекта

Берём: IDEA-Research/grounding-dino-base
Зачем: находит главный объект на исходной фотографии до вырезания фона.
Как использовать: prompt фиксированный — "object.", берём самый уверенный и самый крупный bbox, потом расширяем его на padding под будущую тень. Grounding DINO официально поддерживается в transformers.
Hugging Face

2. Вырезание / маттинг

Берём: ZhengPeng7/BiRefNet_lite-matting
Зачем: основной production matting.
Как использовать: запускаем по crop из детектора, получаем alpha/mask, потом оставляем largest connected component.
Почему именно он: это лёгкая версия BiRefNet с лицензией MIT, то есть её удобно ставить в сервис без лишних лицензионных рисков.
Hugging Face

3. Карта глубины

Берём: depth-anything/Depth-Anything-V2-Small-hf
Зачем: depth для генератора тени и для расчёта нормалей.
Как использовать: запускаем уже на вырезанном объекте или на crop после удаления фона.
Почему именно он: Depth Anything V2 совместим с transformers, быстрее и легче diffusion-based depth моделей, и авторы прямо отмечают, что он более robust и более efficient, чем SD-based depth approaches.
Hugging Face
+1

4. Карта нормалей

В v1 отдельную модель не ставим.
Как делаем: считаем normals из depth через градиенты глубины и нормализацию в camera space.
Почему так: это проще, быстрее и достаточно для production v1. Для сервиса это лучший выбор по сложности и скорости.
Отдельную модель нормалей в online pipeline не используем.

5. Генератор тени

Вместо обученной модели в v1 ставим заглушку.
Как работает заглушка:

берём mask объекта,

сдвигаем её в сторону angle_deg,

масштабируем по elevation_deg,

размываем Gaussian blur в зависимости от softness,

уменьшаем opacity по параметру opacity,

optionally слегка тонируем shadow по параметру reflection,

композитим под объект на фон.

Это и будет временная production-заглушка вместо настоящего shadow generator.

6. Кэш

Кэшируем обязательно:

bbox,

crop,

mask,

cutout,

depth,

normals,

camera params.

Не кэшируем:

финальную тень, потому что она зависит от параметров света.

Что НЕ ставим в v1
BLIP-2

Не ставим.
Он в этом сервисе лишний.

SAM / SAM2

Не ставим в основной путь.
Для этой задачи Grounding DINO + BiRefNet достаточно. SAM тут только усложняет продовый стек.

SD 1.5 / diffusion generator

Не ставим.
На первом этапе генератор — это заглушка с примитивной blur-shadow логикой.

Какой итоговый production stack
Финальный shortlist

Detector: IDEA-Research/grounding-dino-base
Hugging Face

Matting: ZhengPeng7/BiRefNet_lite-matting
Hugging Face

Depth: depth-anything/Depth-Anything-V2-Small-hf
Hugging Face
+1

Normals: вычисляем из depth

Shadow generator: временная заглушка через сдвиг маски + blur

HTTP stack: FastAPI + Pydantic + локальный filesystem cache

Есть ли готовая модель для camera tilt и FOV

Да. Брать надо GeoCalib.

Это не типовой checkpoint из HF model hub, а отдельный проект/репозиторий, но это сейчас самый правильный готовый вариант под твою задачу.
GeoCalib — это single-image camera calibration, который оценивает focal length / FoV и gravity direction, то есть как раз roll/pitch, а в описании и публичных обёртках прямо указано, что он оценивает focal length, FoV, roll и pitch по одному изображению.
GitHub
+2
replicate.com
+2

Что из этого тебе нужно

Из GeoCalib берёшь:

FoV

pitch

roll

Это и есть нужный тебе camera geometry proxy для сервиса.

Как использовать

GeoCalib запускаешь на полном исходном изображении до вырезания объекта.
Это совпадает с твоей логикой: фон и перспектива помогают оценить наклон камеры.

Что не ждать от него

GeoCalib — это не “идеальный предметный студийный калибратор”.
Но для production v1 это лучший готовый вариант, если тебе нужен именно FoV + tilt/roll/pitch по одной картинке.
GitHub
+1

Итоговое решение без колебаний
Ставим

IDEA-Research/grounding-dino-base

ZhengPeng7/BiRefNet_lite-matting

depth-anything/Depth-Anything-V2-Small-hf

GeoCalib

blur-shadow заглушку вместо генератора

Не ставим

BLIP-2

SAM в основной путь

отдельную модель нормалей

diffusion generator на первом этапе

Как должен идти pipeline в коде

Decode image

GeoCalib на полном изображении → fov, pitch, roll

Grounding DINO → bbox главного объекта

Crop с запасом

BiRefNet → alpha/mask

Depth Anything V2 Small → depth

Normals из depth

Кэш preprocess

Primitive shadow stub → shadow layer

Composition

Return final + debug

Самый короткий ответ на твой вопрос

Да, готовая модель для tilt/FOV есть: GeoCalib.
Production v1 stack такой: Grounding DINO Base + BiRefNet Lite Matting + Depth Anything V2 Small + GeoCalib + примитивная shadow-заглушка на blur.
GitHub
+3
Hugging Face
+3
Hugging Face
+3

Если хочешь, я следующим сообщением соберу это уже в виде готового раздела для ТЗ, сухо и формально.

## Turn 39 - User

напиши один полный мд текст по моделям иих пайплайну.
без негативных рекомендаций

## Turn 40 - Assistant

Markdown
# Рекомендуемый стек готовых моделей и pipeline сервиса ShadowGen v1

## 1. Общая схема

Для первой production-версии сервиса рекомендуется использовать следующий фиксированный стек моделей и компонентов:

- **Определение главного объекта:** `IDEA-Research/grounding-dino-base`
- **Оценка параметров камеры:** `GeoCalib`
- **Вырезание объекта / маттинг:** `ZhengPeng7/BiRefNet_lite-matting`
- **Оценка глубины:** `depth-anything/Depth-Anything-V2-Small-hf`
- **Карта нормалей:** вычисляется из карты глубины
- **Генератор тени:** временная заглушка на основе сдвига маски, перспективного искажения и Gaussian blur
- **Слой композиции:** собственный Python-модуль поверх PIL / OpenCV / NumPy

Такой стек позволяет быстро собрать рабочий сервис, сохранить модульность и подготовить инфраструктуру для последующей замены shadow-заглушки на обученную модель.

---

## 2. Модель определения главного объекта

### Выбранная модель
`IDEA-Research/grounding-dino-base`

### Назначение
Модель используется для определения главного объекта на исходной фотографии до удаления фона.

### Причина выбора
Grounding DINO поддерживается через `transformers` и предназначен для open-set / zero-shot object detection, что позволяет использовать фиксированный prompt вида `"object."` и получать bbox основного объекта без дополнительной captioning-модели.

### Как использовать
1. Сервис получает исходное изображение.
2. В модель передается изображение и фиксированный текстовый prompt:
   - `"object."`
3. Из всех найденных bbox выбирается основной:
   - по максимальной уверенности;
   - при близких confidence — по наибольшей площади.
4. Bbox расширяется на безопасный padding под будущую тень.
5. Полученный bbox используется как основа для дальнейшего crop.

### Выход модели
- `bbox`
- `detection_confidence`

---

## 3. Модель оценки параметров камеры

### Выбранная модель
`GeoCalib`

### Назначение
GeoCalib используется для оценки параметров камеры по исходному изображению до вырезания объекта из фона.

### Какие параметры используются
Из результатов GeoCalib сервис использует:
- `fov` / `focal length proxy`
- `pitch`
- `roll`

GeoCalib является моделью single-image camera calibration и оценивает focal length / field of view и ориентацию камеры относительно гравитации, включая pitch и roll.

### Как использовать
1. Исходное изображение до кропа и удаления фона передается в GeoCalib.
2. Из результата извлекаются:
   - `camera_fov`
   - `camera_pitch`
   - `camera_roll`
3. Эти параметры сохраняются в preprocessing cache и дальше передаются в генератор тени.

### Выход модели
- `camera_fov`
- `camera_pitch`
- `camera_roll`

---

## 4. Модель вырезания объекта и маттинга

### Выбранная модель
`ZhengPeng7/BiRefNet_lite-matting`

### Назначение
Модель используется для получения точной маски и alpha matte главного объекта.

### Причина выбора
BiRefNet Lite Matting опубликован на Hugging Face, имеет лицензию MIT и предназначен для matting / dichotomous segmentation. Это делает модель удобной для production-встраивания в Python-сервис.

### Как использовать
1. После детекции объекта исходное изображение кропится по bbox с запасом.
2. Crop передается в BiRefNet Lite Matting.
3. На выходе сервис получает:
   - alpha matte;
   - бинарную маску.
4. Далее выполняется постобработка:
   - largest connected component;
   - удаление мелкого мусора;
   - optional feathering на границе.

### Выход модели
- `mask`
- `alpha`
- `cutout_rgba`

---

## 5. Модель оценки глубины

### Выбранная модель
`depth-anything/Depth-Anything-V2-Small-hf`

### Назначение
Модель используется для получения monocular depth карты объекта после вырезания из фона.

### Причина выбора
Depth Anything V2 доступен в формате Hugging Face, поддерживается через `transformers` и позиционируется как более быстрый и более легкий depth estimator по сравнению с diffusion-based depth подходами. В model card также отмечается улучшенная детализация depth maps.

### Как использовать
1. После получения `cutout_rgba` сервис формирует RGB crop объекта.
2. RGB crop передается в Depth Anything V2 Small.
3. На выходе получается относительная depth map.
4. При необходимости depth нормализуется в диапазон `[0, 1]`.
5. Depth сохраняется в preprocessing cache.

### Выход модели
- `depth_map`

---

## 6. Получение карты нормалей

### Выбранный подход
Карта нормалей рассчитывается из карты глубины внутри сервиса.

### Назначение
Нормали используются как дополнительный geometric prior для shadow generation.

### Как использовать
1. Depth map преобразуется в поле градиентов по осям X и Y.
2. По градиентам вычисляются surface normals в camera space.
3. Нормали нормализуются и сохраняются в формате `H x W x 3`.
4. Результат кэшируется вместе с depth map.

### Выход
- `normal_map`

---

## 7. Временный генератор тени

### Выбранный подход
На первом этапе вместо обученной генеративной модели используется детерминированная заглушка.

### Назначение
Обеспечить полностью рабочий production pipeline до подключения обученной shadow model.

### Как работает заглушка
На вход подаются:
- `mask`
- `depth_map`
- `normal_map`
- `camera_fov`
- `camera_pitch`
- `camera_roll`
- пользовательские параметры:
  - `angle_deg`
  - `elevation_deg`
  - `softness`
  - `opacity`
  - `reflection`

Далее выполняются шаги:
1. Бинарная маска объекта проецируется в сторону `angle_deg`.
2. Сдвиг и деформация маски зависят от:
   - `elevation_deg`
   - `camera_fov`
   - `camera_pitch`
3. Формируется черновая shadow mask.
4. Мягкость тени задается Gaussian blur, радиус которого зависит от `softness`.
5. Плотность тени регулируется параметром `opacity`.
6. При необходимости добавляется слабый color tint / reflection component.
7. Полученный shadow layer передается на этап композиции.

### Выход
- `shadow_rgba`

---

## 8. Слой композиции

### Назначение
Собрать итоговое изображение из объекта, тени и фона.

### Как использовать
1. Создается фон, заданный параметрами запроса.
2. На фон накладывается слой тени.
3. Затем накладывается вырезанный объект.
4. Формируется итоговый `final` artifact.
5. При включенном debug возвращаются промежуточные артефакты.

### Выход
- `final_image`
- optional debug artifacts:
  - `cutout`
  - `mask`
  - `depth`
  - `normals`
  - `shadow`

---

## 9. Порядок работы pipeline

### Шаг 1. Decode
- декодирование входного изображения;
- проверка MIME и структуры payload.

### Шаг 2. Camera estimation
- запуск `GeoCalib` на полном исходном изображении;
- получение:
  - `camera_fov`
  - `camera_pitch`
  - `camera_roll`

### Шаг 3. Main object detection
- запуск `grounding-dino-base` на полном изображении;
- получение bbox главного объекта.

### Шаг 4. Crop
- расширение bbox;
- подготовка crop с полями под будущую тень;
- приведение к рабочему формату.

### Шаг 5. Matting
- запуск `BiRefNet_lite-matting`;
- получение `mask`, `alpha`, `cutout_rgba`.

### Шаг 6. Depth estimation
- запуск `Depth Anything V2 Small`;
- получение `depth_map`.

### Шаг 7. Normal map
- вычисление `normal_map` из `depth_map`.

### Шаг 8. Preprocessing cache
- сохранение в кэш:
  - bbox
  - camera params
  - mask
  - alpha
  - cutout
  - depth
  - normals

### Шаг 9. Shadow generation
- запуск shadow stub на основе mask/depth/normals/camera params и параметров запроса.

### Шаг 10. Composition
- сборка финального изображения.

### Шаг 11. Response encoding
- кодирование финального изображения;
- сборка debug artifacts при необходимости;
- возврат JSON-ответа.

---

## 10. Кэширование

### Что кэшируется
По ключу вида:

`sha256(image_bytes) + preprocess_version + working_size`

сохраняются:
- `bbox`
- `camera_fov`
- `camera_pitch`
- `camera_roll`
- `mask`
- `alpha`
- `cutout_rgba`
- `depth_map`
- `normal_map`

### Что не кэшируется
- финальная тень;
- финальный композит.

Это позволяет повторно использовать preprocessing при изменении угла и параметров света.

---

## 11. Реализация в коде

### Рекомендуемый runtime
- `FastAPI`
- `Pydantic v2`
- `transformers`
- `torch`
- `opencv-python`
- `Pillow`
- `numpy`

### Использование моделей

#### Grounding DINO
Загружается через `transformers`:
- `AutoProcessor`
- `AutoModelForZeroShotObjectDetection`

#### Depth Anything V2
Загружается через `transformers`:
- `AutoImageProcessor`
- `AutoModelForDepthEstimation`

#### BiRefNet Lite Matting
Загружается из Hugging Face как отдельная matting-модель с кастомным inference wrapper.

#### GeoCalib
Подключается как отдельный модуль single-image camera calibration.

---

## 12. Итоговый рекомендуемый стек

### Основной production stack v1
- `IDEA-Research/grounding-dino-base` — детекция главного объекта
- `GeoCalib` — оценка `camera_fov`, `pitch`, `roll`
- `ZhengPeng7/BiRefNet_lite-matting` — вырезание объекта и alpha matte
- `depth-anything/Depth-Anything-V2-Small-hf` — depth map объекта
- `normal_map from depth` — расчет нормалей в сервисе
- `shadow stub` — временная заглушка генератора тени
- `composer` — финальная композиция на фоне

---

## 13. Роль этого стека в развитии сервиса

Этот набор моделей и компонентов является базовым production pipeline для версии v1 и должен использоваться как стабильная опора для:

- интеграции HTTP API;
- контрактного взаимодействия с клиентом;
- тестирования кэширования;
- замера latency;
- сборки Docker-образа;
- последующего переноса в Triton / model serving;
- последующей замены shadow stub на обученную shadow generation model.

На первом этапе именно этот стек считается основным и обязательным к реализации.

